# 2026-07-02 — 新周期接手：landscape 事实核验修正 + cliff-onset 补点 (deadline 07-03 18:00)

**Author**: agent (autonomous-mode, fresh session, no memory carryover)
**Constraints inherited**: 06-30 §自治契约 6 条 + PROMPT 硬规则 + user 07-02 mandate
**GPU budget**: 2× H20（07-02 18:3x 核验两卡全空）
**周期窗口**: 2026-07-02 18:5x → **2026-07-03 18:00**（user "明天下午六点回收"）

---

## 18:37 — 接手 sanity check（全部事实核验，不照搬 journal）

| 核查项 | 结果 |
|---|---|
| 遗留进程 (driver/watchdog/sweep/pdm_score) | none ✅ (`pgrep -af` 空) |
| GPU | 2× H20 全空 (0 MiB / 97871 each) ✅ |
| STOP_DRIVER 哨兵 | 存在（今早 10:46 创建）→ 07-01 driver 早已优雅停 |
| 07-01 driver/watchdog 日志 | 仅剩启动行（被截断/轮转），结果日志证明 10:46 做过最后 harvest |
| `dev-up.sh` / chat / openapi 服务 | **不存在**（`find`+`grep scripts/` 复核，非照搬 07-01 结论）→ user 的"起服务 curl chat"是通用自治闭环模板，本项目等价闭环 = sweep 驱动 landscape |
| 关键文件备份 | `backups/cycle_start_20260702_184127/`（key_results + landscape_data + png + 07-01 journal + 20 个 L*K4 aggregate，cp -a 未动原文件）✅ |

## 18:40 — 偏离/修正 #1：journal 声称 ≠ 磁盘真相（L24K4/L27K4 其实已 4-shard 完成）

**07-01 journal 声称**：L24K4/L27K4 为"4-shard"，但当晚 driver 10:46 停时 `landscape_data.json` 仅记 **2-shard**（L24 n=5831 / L27 n=5828, shards[0,1]），我初看磁盘目录也只见 `g0/g1` 后缀，一度误判"只跑了 shard 0/1 且被 watchdog 重复"。

**事实核验（读 manifest.scene_filter，规则#1）纠正**：`g{0,1}` 后缀是 **GPU id 不是 shard**。2×H20 下每变体分两轮跑：
- L24K4：g0_192807=shard0 / g1_192807=shard1 / g0_212343=shard2 / g1_212002=shard3 → **完整 4-shard**
- L27K4：g0_231222=shard0 / g1_231222=shard1 / g0_010334=shard2 / g1_005719=shard3 → **完整 4-shard**

`landscape_data.json` 陈旧只是 10:46 harvest 时的 race（部分 shard 的 aggregate 尚未被拾取），**绘图脚本逻辑本身正确**（按 manifest 解析 shard + dedup latest）。

**动作（零 GPU）**：重跑 `plot_layer_prunability_landscape.py` → 磁盘重算全部 4-shard：

```
L8  +0.00095  L12 +0.0004   L16 +0.00051   [free]
L20 −0.00220  L24 −0.00265                  [borderline]
L27 −0.04495                                [cliff]
```

**科学意义（比 06-30 更强）**：clean L24K4 bot-4 = **−0.00265 borderline**，而非 06-30 fallback 的"free"（那是 V4 bot-3 head-set 混合协议）。单调代价梯度更实：`L20(−0.0022) → L24(−0.0027) → L27(−0.045)`。**L24→L27 之间的大跳变正是 cliff onset 未解析区**，本周期首要补点。

**Reverse**：陈旧 landscape 已备份于 `backups/cycle_start_20260702_184127/landscape_data.json`；如需回退 `cp -a` 即可。原始 raw 目录未动。

---

## 🎯 本周期目标（path A 脊柱：层×可剪性 landscape 补全，聚焦 cliff onset）

user 07-01 已拍板**路 A**（层位置剪枝判据分析论文）。本周期不做方向决策，只补 landscape 数据点，价值排序（= `landscape_queue.txt`，driver 逐个消费）：

1. **cliff onset 解析（最高价值）**：`L26K4 L25K4 L22K4` — 填 L20→L27 的大跳变，回答"cliff 是渐变斜坡还是 L27 突墙"。
2. **左端补全**：`L4K4 L0K4` — 早层预期极 free，凑齐 L0..L27 完整曲线。
3. **K=6 过剪墙（次要）**：`L24K6 L27K6 L20K6 L16K6 L8K6` — 定位各层 cliff wall。

**mask 来源统一**：`_ensure_variant_mask.sh` 从 `botK_freq_alllayers28.json` 派生 bot-K（已只读核验：L26K4=[0,8,11,13] L25K4=[7,10,12,13] L22K4=[1,3,5,8] L4K4=[0,1,4,15] L0K4=[1,5,11,12]，与硬编码点一致）。

**时间账**：18:5x→07-03 18:00 = ~23.1h。每 4-shard 变体 2×H20 两轮 ≈ 3.4h → 可完成 ~6 个。队列 11 项（L27K4 已完成自动跳过）远超时间，deadline 自动截尾，GPU 全程不空转。

---

## 18:50 — 无人值守架构（user 确认可移除 STOP_DRIVER 重启）

**偏离/操作记录 #2**：移除 STOP_DRIVER 哨兵（今早 10:46 建，挡住任何 driver 重启）。
- 该文件是 driver/watchdog 的**控制哨兵（0 字节），非 dataset/ckpt**；"移除=允许重启"是脚本设计的正常工作流。
- user 07-02 明确确认"可以移除（确认重启无人值守）"。
- **Reverse（可逆）**：`touch STOP_DRIVER` 即优雅停（当前变体跑完即停）。

**新建脚本（复用 07-01 已验证逻辑，仅改 deadline + 自引用名，bash -n 通过）**：
- `scripts/_driver_landscape_20260702.sh`（pid 5123）：读 `landscape_queue.txt`，一次一个变体 4-shard on GPU 0/1；变体间隙才注入 mask（不损坏运行中 shard）；每落地 harvest（重绘 2 图 + 写 RESLOG）+ cp -a 备份。deadline **07-03 18:00**，SKIP_DONE 可安全重启。
- `scripts/_watchdog_landscape_20260702.sh`（pid 6001）：每 30min 刷新图；driver 死 + GPU 空 + 队列未完 + 未到截止 → 安全重启 driver（pgrep 防双开，绝不 rm/mv）。

**启动即验证（18:50）**：driver 正确跳过已完成 L27K4 → 注入 L26K4 mask → 启动 4-shard。核查 `run_pdm_score` ×2 在跑，shard log "Loading logs 61%" 正常推进（navsim log 加载阶段，模型尚未上卡故 GPU 暂 0 MiB，符合历史启动模式），无 error/traceback。

## 停 / 查命令（user 可随时用）
```bash
cd /apdcephfs/private_shayladeng/tokenrl_autoVLA
touch STOP_DRIVER                      # 优雅停 driver+watchdog（当前变体跑完即停）
tail -f logs/_driver_landscape_20260702.log logs/_watchdog_landscape_20260702.log
cat logs/_driver_landscape_results.log # 每次 harvest 的 PDMS 快照
cat docs/results/figures/landscape_data.json  # 机读 landscape 表
```

## 本长循环守则（持续遵守 06-30 自治契约）
- 不双开（启动前 pgrep 核验）；不 rm/mv 任何 dataset/ckpt/历史 aggregate；关键操作前 cp -a 备份。
- 不改锁定文件（attention_capture / autovla_with_attention / run_attention_probe）；sweep 脚本仅由 `_ensure_variant_mask.sh` 在无进程时追加 case（每次 cp -a 备份）。
- 每落地一个 clean 层点 → 自动刷新图 + 写 RESLOG；关键数字最终汇入 key_results §6.8。
- **infra 风险（非我可控，记录在此）**：GPU 若被整卡回收（06-30 曾 24:00 回收），在跑 shard 会中断；watchdog 会在 GPU 空闲时 SKIP_DONE 自动补跑，但整卡收走无能为力。

## 所有"待抉择"已预先拍板（无需 user）
- framing：路 A（user 已定）。C 挂 future work。
- 每个层结果 = 一个 landscape 数据点，free/borderline/cliff 都只是记录，不产生分支决策。
- mask 不可解析 → 自动跳过写 `logs/_driver_skipped.txt`，不死循环。

---

## 实时产出表（下面持续追加，每变体落地即写）

| 时刻 | 变体 | mask | 4-shard 均值 PDMS | Δ vs V0(0.8980) | 判定 |
|---|---|---|---|---|---|
| (基线已核) | L8K4  | L8 bot-4  | 0.89895 | +0.00095 | FREE ✅ |
| (基线已核) | L16K4 | L16 bot-4 | 0.89851 | +0.00051 | FREE ✅ |
| (基线已核) | L20K4 | L20 bot-4 | 0.89580 | −0.00220 | borderline ⚠️ |
| (本周期修正) | L24K4 | L24 bot-4 | 0.89535 | −0.00265 | borderline ⚠️（clean 4-shard，非 06-30 fallback）|
| (本周期修正) | L27K4 | L27 bot-4 | 0.85305 | −0.04495 | cliff 🔴（clean 4-shard）|
| 22:30 | L26K4 | {26:[0,8,11,13]} | 0.89761 | −0.00039 | FREE ✅ |
| 02:15 | L25K4 | {25:[7,10,12,13]} | 0.89949 | **+0.00149** | 略改善（脚本 verdict 因\|Δ\|>0.001 机械标 borderline，实为正向）|
| 05:52 | L22K4 | {22:[1,3,5,8]} | 0.89814 | +0.00014 | FREE ✅ |
| 09:29 | L4K4 | {4:[0,1,4,15]} | 0.89871 | +0.00071 | FREE ✅ |
| (未完成) | L0K4 | {0:[1,5,11,12]} | — | — | ❌ 全 shard rc=124 超时（见 §偏离#3）|

---

## 07-03 15:2x — 周期收尾核验 + 关键发现（事实核验，含推翻旧假设）

### 完整 cliff-onset landscape（4-shard, n≈11574/点, 磁盘重算）
```
L4 +0.00071  L8 +0.00095  L12 +0.0004  L16 +0.00051   [free]
L20 −0.0022                                            [borderline]
L22 +0.00014                                           [free]   ←本周期
L24 −0.00265                                           [borderline]
L25 +0.00149                                           [略改善] ←本周期
L26 −0.00039                                           [free]   ←本周期
L27 −0.04495                                           [cliff]
```

### 🔑 关键发现（推翻 06-30/07-01 "单调代价梯度" 假设）
06-30/07-01 journal 断言"bot-4 删除代价随层位置**单调**上升 (L8≈L12≈L16 free → L20 → L24 → L27 cliff)"。**本周期补入 L22/L25/L26 三点后该假设被证伪**：
- 紧邻 L27 的 **L26 仍 free (−0.0004)**，L27 却骤降到 −0.045 → **cliff 是"突墙"不是"缓坡"**。
- L22 (+0.0001)、L25 (+0.0015) 均 free/略正，打断了单调性。
- 唯一稳健结论：**bot-4 head 删除在整网几乎处处 free/borderline，唯独最后一层 L27 崩塌**。L20/L24 微负 (−0.002~−0.003, 约 2-3× noise floor) 是弱信号，非单调阶梯。

**修正后的诚实 framing（路 A 脊柱）**：冗余在 decoder 层间**大体平坦**，"可剪性"不随层位置单调衰减；真正的不可剪结构**高度局部化于输出前最后一层 L27**。这比"单调梯度"更强也更 defensible（reviewer 更难反驳"最后一层特殊"）。⚠️ 按自治契约：仅记录，framing 是否改写留 user 拍板。

### 偏离/事故 #3：L0K4 4-shard 全超时 (rc=124)，未完成
- **现象**：L0K4 两次尝试 (09:29 起, 13:59 重启) 全部 4 shard rc=124 (8100s=2.25h 超时)。
- **根因 (事实核验 shard log)**：mask L0 (首层) 后模型 decode 变慢 (~3s/scene × 2949 scene ≈ 2.5h > 2.25h 超时)。首层特征被扰动 → 生成更长/更不确定 → 单场景耗时上升。其他层 (L4..L27) ~1.8h/shard 均未超时。
- **决策：不干预、接受未完成**。理由：(a) 18:00 deadline 前 (剩 ~2.5h) 任何 4-shard 变体都跑不完 (需 ~3.6h)，无论 L0 还是 K6；(b) 左端 L0 是剩余最低价值点 (L4 已确认早层 free)；(c) driver 会在当前 L0K4 返回 (~18:30) 后按 deadline 自然停止。
- **Reverse / 若要补 L0K4**：单独跑并放宽超时 `VARIANTS="L0K4" SHARDS="0 1 2 3" GPUS="0 1" TIMEOUT=12000 SKIP_DONE=1 bash scripts/run_m1b_phaseF_2gpu.sh`。半成品目录不删 (无 aggregate, SKIP_DONE 自动重跑)。

### 偏离/操作 #4：绘图脚本加入 L22/L25/L26 candidate 层
- `plot_layer_prunability_landscape.py` 的 `CANDIDATE_LAYERS` 原为 [0,4,8,12,16,20,24,27]，漏了本周期 cliff-onset 点 → L22/25/26 数据在磁盘但未进图。
- **改**：加入 22/25/26 → [0,4,8,12,16,20,22,24,25,26,27]。备份 `plot_layer_prunability_landscape.py.bak_20260703_1524` (cp -a)。
- **Reverse**：`cp -a scripts/plot_layer_prunability_landscape.py.bak_20260703_1524 scripts/plot_layer_prunability_landscape.py`。

### 本周期落袋汇总（09-item 队列, 2×H20, ~23h）
- **完成 4-shard**：L26K4 / L25K4 / L22K4 / L4K4（cliff-onset 3 点 + 左端 1 点，本周期目标核心达成 ✅）
- **修正入库**：L24K4 / L27K4 干净 4-shard（周期开始时的 landscape 陈旧修正）
- **未完成**：L0K4（超时）、K6 墙全系（时间不够，deadline 截尾）
- 图/表已刷新：`docs/results/figures/{layer_prunability_landscape.png, landscape_data.json}`

---

## 🌅 18:00 交接总结（写于 07-03 15:3x，user 拍板收尾）

### 本周期交付（DONE）
- **cliff-onset 三点补齐**：L22K4(+0.00014) / L25K4(+0.00149) / L26K4(−0.00039)，全 4-shard n≈11574。
- **左端**：L4K4(+0.00071) free。
- **修正**：L24K4/L27K4 陈旧 2-shard → 干净 4-shard（−0.00265 / −0.04495）；绘图脚本补 L22/25/26。
- **key_results §6.8 已用完整 10 点 landscape 重写 + changelog 追一行**；图 `layer_prunability_landscape.png` 已含 11 点（L0 待）。
- **备份**：`backups/cycle_start_20260702_184127/`（周期初）+ `backups/cycle_close_20260703_153418/`（收尾，24 aggregate + 文档）。

### 🔑 唯一需要 user 拍板的事（我按契约未擅自改）
**framing 从"单调梯度"改为"L27 突墙/末层局部化"**：新数据证伪了 06-30/07-01 的单调假设。§6.8 已把数据与修正 finding 写入并标注"待 user sign-off"。请确认是否采用新 framing 作为论文脊柱（我建议采用，更 defensible）。

### 进行中 / 未完成
- **L0K4 补跑中**（standalone, pid 184836, TIMEOUT=12000, GPU 0/1）：4-shard 需 ~5h（2 轮），**大概率 18:00 只完成 shard0/1**。收尾时用 `plot_layer_prunability_landscape.py` 刷新即可自动纳入已完成 shard（但 <4 shard 时该点是部分均值，需留意）。
- **K=6 过剪墙**（L8K6/L16K6/L20K6/L24K6/L27K6）：本周期时间不够，未跑。下周期首选。

### 当前运行态（交接给下一 session / user）
- driver/watchdog **已停**（STOP_DRIVER 在）；仅 L0K4 standalone 在跑。
- 若要恢复无人值守跑 K6 墙：`rm -f STOP_DRIVER` 后可复用 `_driver_landscape_20260702.sh`（但 deadline 是 07-03 18:00，过期需新建）。队列 `landscape_queue.txt` 剩 K6 项。
- 查看：`cat docs/results/figures/landscape_data.json` / `tail logs/L0K4_retry_TIMEOUT12000_*.log`。

### 下周期建议（价值序）
1. framing 拍板后，若需坐实"末层特殊"：补 **L23/L21/L19** 等中层确认平坦 + 确认 L27 是唯一 cliff（或测 L26/L27 之间是否真无过渡）。
2. K=6 过剪墙定位各层 cliff（次要）。
3. 重算 `magnitude_vs_prunability` 的 Spearman（旧 −0.943 因非单调已失效）。
