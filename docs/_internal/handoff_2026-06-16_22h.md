# Handoff @ 2026-06-16 ~22:00 回收点

> 📊 **B0 关键数字 / paper 对比** → [`docs/results/key_results.md`](../results/key_results.md)
> 写于 19:48，预计 22:00 用户回收。本文档目的：让你（或下一段对话的 agent）
> 5 分钟内 onboard 当前状态。

## TL;DR — 一句话

**navtrain 下载 + 自动 install + sanity + M0.2 切分**全 chain 已挂在
后台，pid 57067 (chain) + 56276 (aria2c)。3 个可能的状态：

| 状态 | 判断方式 | 含义 |
|---|---|---|
| ✅ 全绿 | `_staging_navtrain/.chain_complete` 存在 | M0 全部 done，可直接进 M1 |
| ⚠️ chain 失败 | `_staging_navtrain/.chain_failed` 存在 | 看 `logs/post_dl_chain.log` 末尾 |
| ⏳ 还在跑 | 上面两个文件都不在，pid 仍活 | 看 aria2c 进度 |

```bash
# 一键判断 22:00 状态：
ls -la /apdcephfs/private_shayladeng/tokenrl/data/navsim_v2/_staging_navtrain/.chain_*
ls -la /apdcephfs/private_shayladeng/tokenrl/data/navsim_v2/_staging_navtrain/.download_complete
ps -p 56276 57067 -o pid,etime,stat 2>/dev/null
```

## 当前在跑的两条后台进程

### pid 56276 — aria2c 下载
- 脚本: `scripts/download_navtrain_robust.sh`
- 日志: `logs/navtrain_download_aria2.log`
- 当前状态 (19:46): 第 1 / 8 个 tgz @ 40%（navtrain_current_1.tgz, 50GB）
- 速度: ~100 MB/s（比 wget 快 100×）
- ETA: 8 个 tgz × ~10min/tgz + extract/rsync ≈ **总 ~2h10min**
  → **预计 21:55 左右完成**（卡 22:00 临界）
- 完成时写: `_staging_navtrain/.download_complete`

### pid 57067 — chain watcher
- 脚本: `scripts/post_dl_chain.sh`
- 日志: `logs/post_dl_chain.log`
- 行为: 每 60s poll `.download_complete`，检测到立即跑：
  1. `install_navtrain.sh`（mv staging → live, ~10min, 同盘 mv 是 atomic）
  2. `check_navtrain_sanity.py`（SceneLoader 加载 + driving_command 分布, ~3min）
  3. `build_m02_splits.py`（probe_A.txt + train/val_pool, ~10min）
- 失败任一步 → 写 `.chain_failed`，停下来不继续
- 全部 OK → 写 `.chain_complete`，输出 `data/splits/{probe_A,train_pool,val_pool}.txt`

## 22:00 之后 3 种处理方式

### 情况 A：22:00 看到 `.chain_complete` ✅
**M0 验收完整通过，可直接进 M1：**
- 检查 `data/splits/probe_A.txt` 应该有 100 行
- 检查 `data/splits/train_pool.txt` + `val_pool.txt` 总和 ≈ navtrain token 数 - 100
- 看 `data/splits/m02_split_stats.txt` 里 `driving_command_dist` —— 这是 plan §Q4.4.a
  里的 4 类分布。如果只有 3 类（很常见，OpenScene 没掉头），probe 自动是 3×25=75，要么改 `--probe-per-class 33`。
- 下一步：开 M1.a 实施（设计 doc 已就位 `docs/_internal/m1_attention_hook_design.md`）

### 情况 B：22:00 看到 `.chain_failed` ⚠️
看 `logs/post_dl_chain.log` 倒数 50 行，最常见 4 种：

1. **"FATAL: ${LIVE_LOGS} already exists and is not empty"**
   → 之前跑过 install。手动删 stale 数据后重跑 install_navtrain.sh：
   ```bash
   rm -rf /apdcephfs/private_shayladeng/tokenrl/data/navsim_v2/navsim_logs/trainval
   rm -rf /apdcephfs/private_shayladeng/tokenrl/data/navsim_v2/sensor_blobs/trainval
   bash scripts/install_navtrain.sh
   ```

2. **"navsim import failed"**
   → PYTHONPATH 没设对，chain 脚本的 `NAVSIM_DEVKIT_ROOT` 路径写死了
   `code/third_party/AutoVLA/navsim`，verify 这个路径还在。

3. **"navtrain token count seems low"**
   → sanity 是 WARN 不是 FATAL，会继续。除非 < 80000 才 fatal。
   实际 navtrain ≈ 103k 左右，正常。

4. **download_died_without_sentinel**
   → aria2c 在中途失败了。看 `logs/navtrain_download_aria2.log` 末尾找原因。
   阶段化 sentinels 在 `_staging_navtrain/trainval_sensor_blobs/.*.installed`，
   下次重跑 `download_navtrain_robust.sh` 会从未完成的 tgz 续。

### 情况 C：22:00 还在跑 ⏳
- 大概率：第一个 tgz 因网速波动慢了，整体延后 20-40min
- 看 `tail logs/navtrain_download_aria2.log` 确认 ETA
- 如果你要立即 review，**不要 kill 任一 pid**，让它继续跑
- chain pid 57067 会自动等下载完，无须管

## 关键事实（避免重复发现）

1. ✅ **B0 已 lock**: PDMS=0.8983 (`docs/journal/MA2_b0_navtest.md`)
2. ✅ **2 invalid token 根因找到**：autovla.predict 返回 pose 数 < 8（hallucination 边界），fix 推迟到 M5/M6 一起改
3. ✅ **-20 token gap 根因找到**：navsim SceneFilter has_route + num_frames 自动 filter，正常行为
4. ✅ **wget 在 S3 EU 上是 1MB/s 大坑**，aria2c x16 = 100MB/s，**永远不要再用 wget 拉 S3**
5. ✅ **OPENSCENE_DATA_ROOT** = `/apdcephfs/private_shayladeng/tokenrl/data/navsim_v2`
   （不是 tokenrl_autoVLA/data，老 navtest 数据在那，要兼容）
6. ✅ **SceneFilter.num_history_frames=4** 默认，意味着 navsim 必须有 history sensor blobs
   → 不能省 history 4×54GB，445GB 全要

## M1 切入预备（可立即开始的事）

如果 22:00 chain 已绿、还想干活：
- 读 `docs/_internal/m1_attention_hook_design.md`
- 打开 `code/third_party/AutoVLA/models/autovla.py` line 522 (`vlm.generate` 调用)
- M1.a 第一步是把 attention dump hook 加上，设计已选 Path C（patch attn 计算只存 1 行 vector）

## TODO snapshot

```
[in_progress] navtrain_dl: 1/8 tgz @ 40%, ETA 21:55
[in_progress] prep_handoff: 此文档
[pending]    post_dl_install/sanity/m02: 由 chain watcher 自动触发
```

## 文件一览（本次会话产生）

```
scripts/download_navtrain_robust.sh   # aria2c x16 下载
scripts/install_navtrain.sh            # mv staging -> live (修了套娃 bug)
scripts/check_navtrain_sanity.py       # 加载 + 分布检测
scripts/build_m02_splits.py            # probe_A + train/val pool
scripts/post_dl_chain.sh               # watcher chain（无人值守）

docs/journal/2026-06-16_b0_invalid_token_diagnosis.md
docs/journal/2026-06-16_navtrain_aria2c_speedup.md
docs/_internal/m1_attention_hook_design.md
docs/_internal/risk_navtrain_data_missing.md
docs/_internal/status_2026-06-16_evening.md   # 上一份 status
docs/_internal/handoff_2026-06-16_22h.md      # 本文档
```

—— 写于 19:48
