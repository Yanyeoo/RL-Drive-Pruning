"""M1 — Stage A: importance scorer pipeline.

Submodules:
- attention_capture: M1.a / M1.b — extract per-vision-token attention
  importance from a frozen Qwen2.5-VL forward pass (Path C design).
"""
