# 2026-07-13 — W5 窗口（τ-cut shard0 quick test + FastV r=0.75 + MSE eval）

**Author**: agent (autonomous-mode)
**读物前置**: `2026-07-10.md` → `aaai_proposal_for_advisor.md` → `key_results.md` → `README.md`

---

## 0. 本周期守则（user 明确，逐条遵守）
1. 每个决策先做事实核验，不"自信但没核"。
2. 起任何进程前先 `pgrep` 查残留，避免双开。
3. 关键 artifact 操作前先 `cp -a` 备份（不动原文件）。
4. 任何偏离 PROMPT / design doc 的决策，当场写 journal（附理由 + reverse 指令）。
5. 实时更新 todo，每完成一项 immediate mark。

## 1. 周期启动事实核验（20:09 已核）

- **GPU**：`nvidia-smi` → **2× H20 全空闲（0 MiB / 0%）**。
- **残留进程**：`pgrep run_pdm_score_cot` / python → **无**。安全。
- **哨兵**：仅 `STOP_DRIVER`（老遗留 landscape driver）。无 `STOP_FASTV`/`STOP_TAUCUT`。
- **磁盘**：47% 使用（1.1T 剩余），充裕。
- **备份**：`backups/cycle_start_20260713_200900/`

### 上周期完成状态（7/10 journal 回顾）

| 完成 | 结果 |
|---|---|
| FastV r=0.5 全 4 shard | PDMS=0.8314（比 random 还差 3.2pt！layer-2 attention 信息质量极差） |
| MSE scorer 训练 | pairwise_acc=0.744 vs LambdaRank 0.839（−9.5pt） |
| LambdaRank vs MSE 消融 | 三指标一致确认 LambdaRank 全面优于 MSE |
| scorer r=0.75 全量 | PDMS=0.8983（−0.05pt vs r=1.0，统计不显著，free lunch） |
| 完整 6-arm + FastV 主表 | scorer > attn +0.18pt > random +2.84pt >> FastV −6.06pt |
| 路线 B 决策确认 | τ-cut quick test → gate decision |

### 待完成（本周期目标）

> **约束**：GPU 回收 = 明天(7/14) 晚 20:00（~24h）；2 卡 H20。
> **AAAI DDL**：摘要 7/21 / 全文 7/28（**仅剩 8/15 天**）。

**主目标（Track A）：τ-cut shard0 quick test（Route B gate）**
1. 🔄 跑 τ-cut（MSE scorer, 4 个 τ 值, shard0, 2 GPU 并行）→ ETA ~03:30
2. 出结果后 → gate decision:
   - Win condition: τ-cut @ mean_kr≈0.5 PDMS > 0.8920 (fixed scorer r=0.5)
   - Win → 继续全量 τ-cut
   - Lose → 回退路线 A（clean negative 也有价值）

**次目标（Track B）：补充实验**
3. FastV r=0.75 全 4 shard（补齐论文主表完整 Pareto）
4. MSE scorer PDMS eval shard0（C1 消融数据）
5. 如 τ-cut 赢 → 立即启动全量 4-shard

## 2. τ 值校准（离线，无 GPU）

对 500 navtest scenes 用 MSE scorer 打分后二分搜索 τ：

| target mean_kr | τ | actual mean_kr | std | range |
|---|---|---|---|---|
| 0.40 | -0.1253 | 0.400 | 0.093 | [0.203, 0.761] |
| 0.50 | -0.1487 | 0.500 | 0.088 | [0.307, 0.814] |
| 0.60 | -0.1668 | 0.600 | 0.078 | [0.417, 0.869] |
| 0.70 | -0.1840 | 0.700 | 0.064 | [0.554, 0.919] |

**关键观察**：τ-cut 确实产生了有意义的场景级自适应！
- std=0.06-0.09（显著大于 fixed-r 的 0）
- range 跨度大（e.g. mean_kr=0.5 时 range=[0.307, 0.814]）
- 这证明 MSE scorer 的分数确实有跨帧绝对意义

## 3. 代码变更

### 新增 `select_prune_positions_taucut()`
- 位置：`code/rldrive/agents/token_prune_patch.py`
- 功能：threshold 模式 token 选择（score > τ → keep）
- 含 safety floor（min_keep=36 ≈ 5% of 720）

### 修改 `AutoVLAWithTokenPruneAgent`
- 位置：`code/rldrive/agents/autovla_with_token_prune.py`
- 新增 selector='scorer_taucut' + tau/tau_min_keep 参数
- τ-cut 分支独立于 top-B 逻辑（不共享 keep_ratio）
- Unit test PASS

### 新增 dispatcher
- `scripts/run_taucut_shard0_quick.sh`：4 τ × shard0，2 GPU 并行

## 4. 启动记录

**20:20** — τ-cut shard0 已启动
- GPU0: τ=-0.1253 (target mean_kr≈0.40)
- GPU1: τ=-0.1487 (target mean_kr≈0.50) ← **核心 win condition arm**
- 速率：~4.4s/scene（2-pass）
- Round 1 ETA: ~23:55（kr040 + kr050）
- Round 2 ETA: ~03:30（kr060 + kr070）
- 输出目录：`results/raw/tokenprune_taucut/TC_mse_tau_kr0{40,50,60,70}_sh0.csv`

---

## 偏离记录

### 偏离 #1：τ-cut 使用 keep_ratio=0.5 作为 dummy（不影响结果）
- **原因**：hydra config 需要一个 keep_ratio 值，但 scorer_taucut 模式完全忽略它（代码中 τ-cut 分支在 keep_ratio<1.0 检查之前就 return 了）
- **影响**：无。keep_ratio 在 τ-cut 模式下不参与任何计算
- **Reverse**：无需

---

## 21:12 — 中间 checkpoint

进度确认（跑了 51 min）：
- GPU0: scene 565/2949 (19.2%), τ=-0.1253 (kr040)
- GPU1: scene 568/2949 (19.3%), τ=-0.1487 (kr050)
- 稳态速率：4.42 s/scene（2-pass）
- 无错误/无 OOM

**精确 ETA**：
- Round 1 (kr040+kr050) 完成：~00:08 (7/14)
- Round 2 (kr060+kr070) 完成：~03:50 (7/14)
- Sequencer 自动续跑：FastV r=0.75 / MSE eval / (如 PASS) τ-cut full

**时间预算 (7/14 03:50 → 20:00 deadline = 16h)**：
- If Gate PASS: τ-cut full (3 shards, ~7h) → FastV r=0.75 (4 shards, ~7h) → MSE eval (~3.5h)
  - 可完成: τ-cut full + FastV r=0.75（刚好 14h 内）
- If Gate FAIL: FastV r=0.75 (~7h, 10:50 完) → MSE eval (~3.5h, 14:20 完) → 剩余时间 backup
  - 充裕完成所有补充实验

**全部 GPU 排满，无空闲**。

---

*自动化运行中，无需人工干预。Sequencer 负责串接。*

