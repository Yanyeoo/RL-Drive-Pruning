"""M1.b₂ probe modules.

Note: this dir is NOT importable as `code.rldrive.probes` because
`code/` has no __init__.py (and "code" collides with stdlib). Add
this directory itself to sys.path and `import per_head_binary_probe`.
"""
