# 2026-07-01 — 明早交接执行 (landscape 全景图) + framing 决策待拍板

**Author**: agent (autonomous-mode, fresh session, no memory carryover)
**Constraints inherited**: 06-30 journal 自治契约 6 条 + PROMPT_FOR_TOMORROW 硬规则
**GPU budget**: 2× H20, 两卡全空 (07-01 18:1x 核验)

---

## 18:1x — 接手 sanity check (全部事实核验, 不照搬)

| 核查项 | 结果 |
|---|---|
| 遗留进程 (chain/sweep/pdm/probe) | none ✅ (`pgrep -af` 空) |
| GPU | 2× H20 全空 (0 MiB / 97871 MiB each) ✅ |
| chain `_chain_xlayer_batch23.sh` | DONE ✅ |
| 午夜收割 (L8/L16/L20 K4) | 全部 4-shard 落盘, 数字与 06-30 一致 ✅ |
| Lcomb4K4 | 4 目录存在但 **aggregate 全 NO** → 午夜砍断 0/4 (符合 06-30 §偏离#5 预测) |
| L*K6 / Lcomb3K4 | 从未产生 (chain 午夜排队被砍) |
| 关键文件备份 | `backups/morning_20260701_181404/` (12 aggregate + journal + key_results), cp -a 未动原文件 ✅ |

## 18:1x — 关于 user 的 "dev-up.sh + curl chat openapi" 规则 (澄清, 非偏离)

**事实核验**: 全项目 (`find -maxdepth 3` + `grep -rl dev-up`) **不存在** `scripts/dev-up.sh` / 任何 chat / serve / openapi 服务脚本; "dev-up" 仅出现在 journal 备份里对模板的引用。

**判定**: user 传的 "起 dev-up.sh → 看日志 → curl chat openapi → check → 循环到符合预期" 是 06-30 journal §🔁 已入册的**通用自治工作模式模板** (原话: "本项目的等价闭环 = chain 驱动 sweep, 我自动读 PDMS → 写 journal/key_results → 价值优先选下一个变体, 无需人盯"). 本项目无 web 服务, 故按等价闭环执行研究交接, 而非字面起 chat 服务。**如 user 确实指另一个 chat 服务项目, 请给出该项目路径, 我再切过去。**

---

## 18:1x — 明早第一件事 (按 06-30 交接): 层×可剪性全景图

**遵循 06-30 明确指令**: "别再惯性多测 free-lunch 层 (边际价值低); 优先把 L8/L12/L16/L20/L24/L27 全部数据拼成一张层×可剪性全景图 (x=层 idx, y=ΔPDMS), 复现/放大 L27 cliff。" → 本动作**遵循最新 journal, 非偏离**; 且无需 GPU、路 A/C 皆用得上。

### 做了什么
1. `scripts/plot_layer_prunability_landscape.py` (新, 可复现): L8/L16/L20 **从磁盘 aggregate 重算 4-shard 加权均值** (规则#1 事实优先, 不照搬 journal 数); L12(Sc4)/L24/L27 用已落盘文档值并标注协议差异。
2. 磁盘重算结果 (印证 06-30 数, 差异仅来自 journal per-shard 四舍五入 vs 我精确加权):
   - L8K4 = 0.8989 (Δ=+0.0009) [journal 0.8990]
   - L16K4 = 0.8985 (Δ=+0.0005) [journal 0.8986]
   - L20K4 = 0.8958 (Δ=−0.0022) [journal 0.8959]
   判定完全一致 (free/free/borderline)。
3. 出图 `docs/results/figures/layer_prunability_landscape.png`。
4. key_results §6.8 新增 landscape 汇总表 + 图引用 + finding; changelog 追一行。

### 全景图结论 (path-A 脊柱)
```
L8(+0.0009) ≈ L12(+0.0004) ≈ L16(+0.0005) [free]
  → L20(−0.0022) [borderline]
  → L27(−0.044)  [cliff]
```
bot-K 删除代价**单调随层位置向输出上升**。且 **magnitude 与可剪性反相关**: L27 g_mean=0.180 (最高) 却最不可剪; L24 g_mean=0.046 (低) 却高度可剪。→ **冗余是层位置结构性的、非 attention-magnitude 的**。这是比"又一层 free"强得多的量化脊柱。

### 数据诚实性 caveat (已在图/表标注)
- L27 只有 V2 混合点 (L12:{h13}+L27:{h0,h8,h9}); L12:h13 本身 free, 故 −0.044 可归因 L27。
- L12 是 2-shard, 其余 4-shard; V0=0.8980 (2-shard ref, 与 4-shard V0=0.8985 在噪声内)。
- L24 头集依赖 (V4 bot-3=−0.0032 vs V3-V2 11-head≈free), 图中作参考点非主线。

---

## 🚦 framing 决策待 user 拍板 (06-30 21:00 遗留, 我不擅自定)

全景图已把两条路的**共用证据**做完。方向选择需 user 拍板:

- **路 A (剪枝判据分析论文, 稳)**: 以"层位置结构性冗余 / magnitude 不预测可剪性"为主线。**下一步**可做: (a) 补 L24/L27 的**纯 bot-4** 干净点消除协议 caveat (~2 卡 ×1.7h/层, 确定产出); (b) per-head magnitude vs prunability 散点图坐实反相关。
- **路 C (online real-reward dynamic, 险但重)**: 需 online 训练 pipeline (offline probe 天花板已证 K_eff=4=静态 Sc4, 见 06-30 §偏离#3/#4)。工程量大、风险高。

**我的建议 (待 user 否决)**: A 为主线拿稳论文, C 挂 future work。若 user 认可路 A, 我可**立刻**用现有 2× H20 起"L24/L27 纯 bot-4 干净点"补测 (确定产出, 消 caveat), 无需再等决策。

## 本 session 未起任何 GPU 进程
- 全景图为纯 CPU/离线数据聚合; 2 卡仍全空, 留给 user 拍板后的补测。
- 无不可逆操作; 新增文件仅: plot 脚本 + png + journal; key_results 为追加 (原内容未删)。

## 备份位置
- `backups/morning_20260701_181404/` — 12 aggregate + 06-30 journal + key_results (cp -a, 原文件未动)。

---

## 19:xx — user 拍板走路 A + "干到明天18:00不停" → 无人值守自驱动闭环

**user mandate (19:18)**: "a" (走路 A / 补 L24 L27 纯 bot-4)。**(19:40)**: "一直运行到明天下午六点, 中间不能停下来问问题或自行停机"。

### 事实核验 (bot-4 选择规则, 保证 landscape 可比)
- 读 `run_m1b_freelunch_sweep.sh`: L8/16/20 K4 mask **== `const_topK_head_idxs`** (from `botK_freq_alllayers.json`, K=4 capture, 每场景 attn 均值最低4 head 的 across-scene 频率 top-4)。规则验证一致。
- L24 bot-4 = **[0,7,9,10]** (freq 1.0/.989/.937/.535) — 直接来自该 json。
- L27 **不在** json (只 capture [0,4,8,12,16,20,24]) → 写 `scripts/_oneoff_botK_freq_L27.py` 用**同方法**从 24GB dump L27 slice 重算: **L27 bot-4 = [0,8,9,15]** (freq 全 1.0, n=19225)。落 `exp/m1b2_phase2_v0/botK_freq_L27.json`。
- 补 landscape 左端: L0 bot-4=[1,5,11,12], L4 bot-4=[0,1,4,15] (同 json)。

### ⚠️ 偏离/事故 #6: 误 `source` sweep 脚本
核验 mask 时误用 `bash -c 'source scripts/run_m1b_freelunch_sweep.sh; mask_for_variant ...'` → **意外执行了 sweep 默认 V0-V3** (GPU0, 各 30-130s 后 rc=1 快速失败, PDMS=null)。产生空目录 `M1b_freelunch_V{0,1,2,3}_g0_20260701_192*` (agg=NO, `is_done`=false, 不匹配 landscape `L*` glob → **惰性无害, 不污染**)。已核验无遗留进程、GPU 释放后才正式启动。**教训**: 提取函数用 `grep`/DRY_RUN, 不 `source` 会执行 main 的脚本。**Reverse (如需清理空目录, 可逆)**: `mv results/raw/M1b_freelunch_V?_g0_20260701_192* /tmp/`。

### 变体补入 (sweep 脚本, 已备份 `.bak_20260701_addL24L27`)
`L24K4={24:[0,7,9,10]}`, `L27K4={27:[0,8,9,15]}`, `L0K4={0:[1,5,11,12]}`, `L4K4={4:[0,1,4,15]}`。

### 无人值守架构 (automation bridge 不可用 → 自建长驻脚本)
- **L24K4 sweep** (19:28 起, pid 7181/7183): 4-shard, GPU0/1, mask 已 pgrep 核验正确。ETA ~23:00。
- **`scripts/_driver_landscape_20260701.sh`** (driver, pid 14918): 等 L24K4 完 → 串跑队列 `L27K4 L4K4 L0K4 L20K6 L16K6 L8K6`, 每个 4-shard, 跑完 immediate harvest (刷新图+备份+写 RESLOG)。带 `STOP_DRIVER` 哨兵 + 硬截止 **07-02 18:00**。SKIP_DONE=1 可安全重启。
- **`scripts/_watchdog_landscape_20260701.sh`** (watchdog, pid 15153): 每 30min 刷新 landscape 图; driver 死 + GPU 空闲 + 队列未完 + 未到截止 → 安全重启 driver (绝不双开/rm)。长驻到 07-02 18:00。
- **`scripts/plot_layer_prunability_landscape.py`** 改为**自动发现磁盘所有 `L{N}K4`** 4-shard 结果 → 图 + `landscape_data.json`。L24/L27 落地后自动从 fallback 替换为 clean 点。

### 队列价值排序 (path A landscape 补全)
1. L24K4 (跑中) — L24 clean bot-4 (消 V4 head-set 依赖 caveat)
2. L27K4 — L27 clean cliff 点 (消 V2 混合 caveat)
3. L4K4 / L0K4 — 补曲线左端 (早层, 预期极 free), 凑齐 8 层完整曲线
4. L20K6 / L16K6 / L8K6 — K=6 过剪, 定位各层 cliff 墙 (次要, 截止前跑多少算多少)

### 停/查命令 (user 可随时用)
```bash
cd /apdcephfs/private_shayladeng/tokenrl_autoVLA
touch STOP_DRIVER                      # 优雅停止 driver+watchdog (当前变体跑完即停)
tail -f logs/_driver_landscape_20260701.log logs/_watchdog_landscape_20260701.log
cat logs/_driver_landscape_results.log # 每次 harvest 的 PDMS 快照
```

### 不做 / 守则 (本长循环持续遵守)
- 不双开 (启动前 pgrep 核验)；不 rm/mv 任何 dataset/ckpt/历史 aggregate；关键操作前 cp -a 备份。
- 不改锁定文件 (attention_capture/autovla_with_attention/run_attention_probe)。
- 每落地一个 clean 层点, watchdog/driver 自动刷新图并把 PDMS 写 RESLOG; 关键数字最终汇入 key_results §6.8。
- **infra 风险**: 若 GPU 被回收 (06-30 曾 24:00 回收), 在跑 shard 会中断; watchdog 会在 GPU 空闲时按 SKIP_DONE 自动补跑, 但若整卡被收走则无能为力 (记录在此, 非我可控)。

---

## 20:00 — 队列升级为文件驱动 + 通用变体, 保证撑满到 07-02 18:00, 所有决策预拍板

**user mandate (19:52)**: 确认能跑到明天18:00, 且"所有需要我抉择的全部你自动解决"。

### 全 28 层 bot-K 扫描 (scan28)
`scripts/_oneoff_botK_freq_alllayers28.py` → `botK_freq_alllayers28.json` (K=4/6/8, 全 28 层, n_ok=19225)。**完全复现所有硬编码 mask** (L8K4=[11,12,14,15], L12K6=Sc6=[0,2,4,6,13,14] … 逐一核对一致) → 通用方法可信。cliff 定位层新得: L22K4=[1,3,5,8], L25K4=[7,10,12,13], L26K4=[0,8,11,13]。

### 架构升级 (无需再问 user 任何事)
1. **文件驱动队列** `landscape_queue.txt` (driver 每轮重读, 可 live 扩展)。价值排序:
   `L27K4(clean cliff) → L26K4 L25K4 L22K4(cliff 定位) → L4K4 L0K4(左端) → L24K6 L27K6 L20K6 L16K6 L8K6(K6 墙)`。
2. **通用 L{N}K{K} 变体**: `scripts/_ensure_variant_mask.sh` 从 scan28 json 派生任意层任意 K 的 bot-K mask, driver 在**两个 dispatcher 间隙 (无 sweep 进程运行时) 才注入** → 不损坏运行中的 shard (bash 字节偏移安全)。备份 `sweep.sh.bak_ensure_*`。
3. driver 跳过不可解析变体 (写 `logs/_driver_skipped.txt`), 不死循环。

### 时间账 (答 user)
现在→18:00 = 22.0h。L24K4 剩 ~3h + 每变体 ~3.4h → 可完成 ~6-7 个。队列 11 个 **远超时间, deadline 自动截尾, GPU 全程不空转**。

### 所有"待抉择"已预先拍板 (无需 user)
- framing: **路 A** (user 已定 "a")。C 挂 future work。
- 每个层结果 = 一个 landscape 数据点, 无论 free/borderline/cliff **都只是记录, 不产生分支决策**。
- GPU 回收 → watchdog SKIP_DONE 自动补跑。
- mask 不可解析 → 自动跳过。
- 所有关键操作前 cp -a 备份, 绝不 rm/mv, 不双开 (启动前 pgrep)。

### 停/查
`touch STOP_DRIVER` 优雅停; `cat logs/_driver_landscape_results.log` 看 PDMS; 两张图 `docs/results/figures/*.png` 每 30min 自动刷新。
