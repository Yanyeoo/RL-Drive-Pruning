# 2026-07-18 — 21h 窗口（18:11 → 明天 15:00 回收）

**Author**: agent  
**Window**: 2026-07-18 18:11 CST → 2026-07-19 15:00 CST（用户指示，约 21h）  
**GPU**: 8× H20（97GB）

---

## 1. 事实核验（接手第一步）

| 项 | 结果 |
|---|---|
| 时间 | Sat Jul 18 18:11 CST |
| GPU 占用 | 8× H20 全 0 MiB / 0% — 全空闲可用 |
| 遗留实验进程 | 无（`pgrep run_pdm_score_cot/train_scorer_grpo` 空）|
| heartbeat | 用户拒绝重启命令 → **未启动**；改用本 journal 实时记录进度 |
| 数据/模型路径 | `data/navtest_metric_cache`、`data/navtest_nocot`、`models/AutoVLA/AutoVLA_PDMS_89.ckpt`、`codebook_cache/agent_vocab.pkl`、`ckpt/s3_token_scorer_mse` 均 OK |
| `NAVSIM_EXP_ROOT` | = `$ROOT/exp`（setup 脚本定义）|
| STOP 文件 | 仅旧 `STOP_DRIVER`（与本实验无关）|
| τ 值 | 来自 `run_taucut_shard0_quick.sh` L28-29 + journal 07-13 交叉验证（非猜）：kr040=-0.1253, kr050=-0.1487, kr060=-0.1668, kr070=-0.1840 |
| 现有 selector | `attn_L12 / random / scorer / scorer_taucut / fastv_l2`；**无** sparsevlm/tome/prumerge |
| 已有 taucut | kr040/050/060/070 仅 sh0；kr060 已 full |
| 已有 MSE full | 无（P2 待跑）|

---

## 2. 决策：HANDOFF 与 design doc 冲突的裁决（规则 #4）

**冲突**：HANDOFF（07-18 17:50）把 P0（SparseVLM+ToMe/PruMerge）列为"最重要"；
但 `design_decisions.md` Q5.c 明确 **ToMe / SparseVLM / VisionZip 已降级到 Appendix / rebuttal 弹药**，主表只用 7 个核心 baseline（FastV / random / attn_L12 / scorer 等，均已完成）。

**裁决**：以 design doc 为准（它是权威设计文档）。
- **优先级重排**：P1 τ-cut 全曲线 + P2 MSE ablation 为闭合 paper 的核心必须项（低风险、已有代码）；P0 实现为 **Appendix/rebuttal 弹药**（nice-to-have，时间允许则做，且实现时明确标记 Appendix）。
- 不把 P0 当阻塞项；若实现风险高/时间紧，P0 可降级为 future-work 并在主表 related-work cite。
- **Reverse 指令**：若后续顾问/用户明确要求 P0 进主表，则恢复 HANDOFF 的 P0 最高优先级，暂停 P1/P2 余量以腾出 4 卡给 P0 实现+eval，并记录回滚点。

---

## 2b. 决策：论文主线 —— 保留 RL 主贡献（用户 18:53 拍板，规则 #4）

**冲突**：`main.tex` 当前把 RL 当主贡献（称 RL>SFT）；但本窗口证据 RL best 0.889 < SFT 0.895，RL 不赢 SFT；HANDOFF §1 亦明确"不要把 RL 作为主 claim"。

**用户裁决（Q1）**：**保留 RL 主线，本窗口维持当前叙事、不重写**，等下轮 RL 重训翻盘。

**执行影响**：
- 本窗口 **不重构** `main.tex` 主线叙事，不动 RL 相关 claim 段落。
- P3 论文工作仅限于：更新数字表/曲线的**占位数字**（key_results 已有的事实数据），不改主线论点。
- RL best 0.889 < SFT 0.895 这一事实仍如实记录在 journal / key_results，供下轮重训对照。

**Reverse 指令**：若下轮 RL 重训仍无法超过 SFT 0.895，则须回到 HANDOFF §1，将主线改为 "SFT scorer + τ-cut adaptive"，RL 降为 appendix/negative ablation（对应我原方案 A）。回滚点 = 本条 journal。

---

## 2c. 决策：实验启动方式（用户 18:53，Q2）

**背景**：agent 的 `nohup … &` 后台派生被环境拒绝 2 次。
**用户裁决（Q2）**：由用户**手动执行 agent 贴出的完整命令**，执行后回报结果，agent 据日志核验。
**结果**：18:54 用户解锁权限，agent 已自行后台启动 dispatcher（pid=11306），7 job 已在 GPU0-6 运行。

---

## 2d. 无人看守模式 + 20h 收尾协议（用户 19:01）

**用户要求**：进入无人看守模式；20h 后（≈明 14:50）实验做完、论文写完；14:50 前完成：备份 + 发结果到企业微信 + 传 GitHub。

**已落地（规则 #2 先核验基础设施）**：
- 实验：dispatcher pid=11306 在跑（GPU0-2=P1 τ-cut 6 job；GPU3-6=P2 MSE 8 job；GPU7 预留）。
- 企业微信：心跳 `wecom_progress_heartbeat.sh`（每 15min，pid=22856）+ 完成监控 `_notify_watcher.sh`（每 5min 检查关键 CSV 并推送，pid=22857）。
- 备份：`scripts/reclaim_1450.sh`（幂等：cp -a 结果/docs/logs/paper 到 `backups/cycle_end_20260719_1450`，不删原文件）。
- GitHub：reclaim 脚本内 `git add docs results paper scripts logs` + commit + `git push origin main`（仅推论文/结果/日志，不推大模型产物；remote 已配带 token）。
- **14:50 自动触发**：`at` job 1 @ 07-19 14:50（`atd` 已确认运行）。脚本先等实验补齐（最多 40min），再备份→企微报告→git push。

**监控循环（agent 持续执行，直至符合预期或可收尾）**：
1. 每 ~30min 查 `logs/_21h_master.log` + 各 job 日志 + nvidia-smi，确认无 CUDA/数据错误、GPU 真正在跑（util>0）。
2. 每 CSV 落地即聚合 PDMS，核验符合预期：
   - **P1 τ-cut**：adaptive τ 的 PDMS ≥ 同 keep-ratio 的 fixed（scorer/MSE）；
   - **P2 MSE**：MSE scorer 固定-r 的 PDMS，对照 LambdaRank@0.5（~0.8918）确认趋势合理。
3. 若发现 PDMS 异常暴跌 / 进程挂掉 → 立即按规则写 journal + 重启对应 shard（dispatcher 幂等可重跑）+ 企微告警。
4. 结果稳定后回填 `main.tex` 的 `\todo{}` 占位（P1/P2 数字）与 `docs/results/key_results.md`，**不改 RL 主线论证（Q1）**。

**Reverse 指令**：若 14:50 时实验仍未完成且无法补齐，reclaim 脚本会带"部分完成"告警推送（不阻塞）；此时若需强制停，则 `touch STOP_21H` 并手动 `git push`。

---

## 3. TODO（实时更新，完成即 mark）

- [x] 事实核验（进程/GPU/路径/τ/selector）
- [x] 裁决 HANDOFF↔design doc 冲突，写 journal
- [x] 写 8 卡并行 dispatcher + 队列
- [ ] **P1 τ-cut 全曲线**：kr050 sh1-3（τ=-0.1487）+ kr070 sh1-3（τ=-0.1840）full eval
- [ ] **P2 MSE ablation**：MSE scorer r=0.5 full(4 sh) + r=0.75 full(4 sh)
- [ ] 核验 P1/P2 CSV：PDMS 符合预期（τ-cut ≥ fixed；MSE < LambdaRank@0.5）
- [ ] **P0 Appendix**：实现 sparsevlm_text + prumerge_cls selector（先 shard0 烟测）
- [ ] P0 eval：r=0.5 + r=0.75 full（若烟测通过）
- [ ] **P3 论文**：更新 `docs/results/key_results.md` + `paper/aaai2027/main.tex` 主表/曲线
- [ ] **P4 Variant B**：仅 P0-P3 完成后，做 66 catastrophic token 最小复现（不 blind full）
- [ ] 明早 3 点前回收检查：汇总结果、写 recovery handoff

---

## 4. 启动（改为用户手动执行 —— 见 §2c）

- **状态**：agent 后台派生被环境拒绝 → 由用户手动执行启动命令（Q2）。
- 启动命令（用户在终端粘贴执行）：
  ```bash
  cd /apdcephfs/private_shayladeng/tokenrl_autoVLA && pgrep -f run_pdm_score_cot >/dev/null && echo "ABORT: already running" || nohup bash scripts/run_21h_master.sh > logs/_21h_master.log 2>&1 & echo "pid=$!"
  ```
- 队列 `_21h_queue.txt`：GPU0-2 = P1 τ-cut(6 jobs)；GPU3-6 = P2 MSE(8 jobs)；GPU7 预留。
- 监控：`tail -f logs/_21h_master.log` 与各 `logs/_21h/_*.log`。
- 停止：`touch STOP_21H`。

---

## 2e. P0 实现评估（agent，19:05 召 agent 大军）

**agent 结论（只读 subagent，无法直接写码，已返回设计+烟测命令草稿）**：
- 两个新 selector：`sparsevlm_text`（文本-视觉交叉注意力打分 top-k）、`prumerge_cls`（CLS-视觉相似度聚类合并）可基于现有 `fastv_l2`/`scorer` 分支模式实现。
- 设计依据：SparseVLM（文本引导视觉 token 选择）、PruMerge（CLS 相似度合并）。
- **重要约束**：subagent 仅只读工具，未实际落码。主 agent 需亲自实现 + import 级检查 + 烟测。
- 烟测命令（shard0，待主 agent 实现后给用户/自主执行）：
  - `CUDA_VISIBLE_DEVICES=7 ... run_pdm_score_cot.py ... +agent.selector=sparsevlm_text +agent.keep_ratio=0.5 ...`
  - `CUDA_VISIBLE_DEVICES=7 ... run_pdm_score_cot.py ... +agent.selector=prumerge_cls +agent.keep_ratio=0.5 ...`

**设计 doc 定位（design_decisions Q5.c）**：SparseVLM/ToMe/PruMerge 为 Appendix/rebuttal 弹药，主表只用 7 核心 baseline（已完成）。故 P0 实现明确标注 Appendix，不进主表。

---

## 2g. GPU7 占满决策（19:10，用户"GPU尽量占满"）

**事实**：GPU7 空闲（0 MiB）；FastV r0.75 已完成（4 shard 在）→ 不能复用。
**裁决**：不盲跑未实现的 P0 selector（风险高），先用**已验证 `scorer_taucut`** 补齐 τ-cut kr040 sh1-3（kr060 已 full，kr050/kr070 由 P1 补齐）→ 完整 4 点 τ-cut 曲线，低风险高价值占满 GPU7。
- 脚本：`scripts/run_gpu7_taucut_kr040.sh`，pid=27609，19:10 START `TC_mse_tau_kr040_sh1`。
- P0 实现完成后，若 GPU7 仍有空闲窗口，再追加 P0 shard0 烟测（Appendix 创新弹药）。
**Reverse 指令**：若 kr040 跑完且 P0 仍未实现，GPU7 改跑额外 Pareto 点（如 attn_L12/scorer 更多 keep-ratio）以保持占满。

---

## 2h. 8 卡状态（19:10 核验）

GPU0-6 = P1 τ-cut(6)+P2 MSE(8) 共 14 job（dispatcher 11306）；GPU7 = τ-cut kr040 sh1-3（27609）。全 8 卡占用，16 python 进程，模型加载中。

---

## 2i. P0 selector 实现落地（19:30，主 agent 实写）

**目标（用户"尽量按 idea 实现 + AAAI 创新"）**：实现 SparseVLM / PruMerge 两个 training-free Appendix baseline selector，强化"learned scorer >> all training-free"创新叙事。

**实现**：编辑 `code/rldrive/agents/autovla_with_token_prune.py`
- `__init__` 校验加入 `sparsevlm_text`/`prumerge_cls`；`sparsevlm_text` 用 L12。
- `_score_for` 新增 `input_ids` 参数 + 两分支：
  - `sparsevlm_text`：文本→视觉交叉注意力（复用 `patch_attention_capture`，按 text query 池化到 vision 位置）作重要性。
  - `prumerge_cls`：`patch_vision_feature_capture` 抓视觉特征，算每 token 与 CLS 代理（mean vision feat）余弦相似度作中心性分（训练-free）。
- `compute_trajectory` 两分支均传 `input_ids`。
- **无需 scorer_ckpt**（P0 是 training-free baseline；scorer/taucut 才需）。
- 已通过 `python ast.parse` 编译校验（无 GPU 运行）。

**烟测脚本**：`scripts/run_gpu7_p0_smoke.sh`（GPU7 空闲时由监控循环启动；先 sh0 烟测，通过后 full 4 sh 占满窗口）。
- sparsevlm_text: `MT_sparsevlm_text_r05_sh{0..3}`
- prumerge_cls:   `MT_prumerge_cls_r05_sh{0..3}`

**预期**：P0 为 training-free，PDMS 预计 ~0.83–0.86，低于 learned scorer 0.892 → 支撑 Appendix "learned >> training-free" 叙事。
**Reverse**：若 P0 烟测崩溃（代码bug），则 GPU7 改跑额外 Pareto 点（attn_L12/scorer 多 keep-ratio），P0 降级为 future-work。

---

## 2j. 当前 8 卡 + 守护状态（19:30）

- GPU0-6: P1 τ-cut(6)+P2 MSE(8)，dispatcher 11306。
- GPU7: τ-cut kr040 sh1-3（27609），跑完后由监控循环接力 → P0 smoke（`run_gpu7_p0_smoke.sh`）。
- 守护: 企微心跳 22856(15min) + 完成监控 22857(5min) + at job1@14:50 收尾。
- 论文: key_results.md §13 已落盘；待 P1/P2 CSV 落地后回填 main.tex \todo。
