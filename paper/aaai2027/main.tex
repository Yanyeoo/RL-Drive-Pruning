\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027}  % DO NOT CHANGE THIS
\usepackage[hyphens]{url}  % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm}  % DO NOT CHANGE THIS
\usepackage{natbib}  % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing  % DO NOT CHANGE THIS

\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{subcaption}

\pdfinfo{
/TemplateVersion (2027.1)
}

\setcounter{secnumdepth}{2}

% --- Macros ---
\newcommand{\ours}{DrivePrune}
\newcommand{\pdms}{PDMS}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\revscore}{\textsc{Score}}

% ============================================================================
\title{DrivePrune: RL-Optimized Adaptive Vision Token Pruning\\for Autonomous Driving VLAs}

\author{
    Anonymous AAAI Submission
}
\affiliations{
    Paper ID: XXXX
}

\begin{document}
\maketitle

% ============================================================================
\begin{abstract}
Vision-language-action models (VLAs) for autonomous driving encode hundreds of vision tokens per frame, creating a computational bottleneck during LLM inference.
Existing token pruning methods either rely on fixed heuristics (attention scores, reconstruction loss) or learn selection rules through supervised distillation alone---neither directly optimizes for the downstream driving objective.
We propose \ours{}, the first reinforcement learning (RL) framework that directly optimizes vision token selection for driving trajectory quality.
Our method trains a lightweight importance scorer (0.6M parameters) via REINFORCE with the Planning-aware Driving Metric Score (PDMS) as reward, enabling the scorer to discover task-optimal token selection strategies that supervised distillation cannot reach.
Combined with a global threshold ($\tau$-cut), \ours{} achieves scene-adaptive pruning ratios---simultaneously deciding \emph{which} tokens to keep and \emph{how many}---without a separate budget controller.
On NAVSIM navtest ($N{\approx}11{,}576$ scenes), \ours{} with Variant~B (true token drop) delivers \todo{X\%} wall-clock speedup with only \todo{Y}-pt PDMS degradation versus no pruning, dominating all fixed-ratio and heuristic baselines.
Our RL-tuned scorer outperforms its SFT-distilled initialization by \todo{+Z} PDMS points at matched keep-ratio, demonstrating that driving reward provides a learning signal beyond what attention distillation offers.
\end{abstract}

% ============================================================================
\section{Introduction}
\label{sec:intro}

% ¶1 Background: AD-VLA + token bottleneck
Vision-language-action models (VLAs)~\citep{autovla2025} are emerging as a powerful paradigm for end-to-end autonomous driving, unifying perception, reasoning, and action generation within a single foundation model.
However, these models encode multi-view camera inputs into hundreds of vision tokens (e.g., 720 tokens from 3 cameras in AutoVLA), creating a significant computational bottleneck during LLM inference.
Reducing vision token count at the ViT$\to$LLM interface is a practical necessity for real-time deployment.

% ¶2 Status quo: heuristic and SFT-only methods
Recent work has explored vision token pruning for driving VLAs.
FastDriveVLA~\citep{fastdrivevla2026} uses MAE reconstruction to identify foreground tokens;
Prune2Drive~\citep{prune2drive2026} employs farthest-point sampling for diversity-aware selection;
ST-Prune~\citep{stprune2026} leverages spatial-temporal redundancy heuristics.
While effective, these methods share a fundamental limitation: their token selection criteria are either hand-crafted (attention scores, coverage metrics) or learned via \emph{supervised distillation alone}, which can only approximate existing signals---never surpass them.

% ¶3 The gap: no RL for AD token pruning
In the general VLM setting, TOP-RL~\citep{toprl2026} recently demonstrated that RL can improve token pruning for VQA tasks.
However, \emph{no prior work applies RL-based token selection to autonomous driving VLAs}, where the reward signal is fundamentally different: rather than answer accuracy, we need trajectory quality measured by composite driving metrics (collision avoidance, progress, comfort).

% ¶4 Our insight and approach
We address this gap with \ours{}, a two-stage framework:
\begin{enumerate}[nosep,leftmargin=*]
\item \textbf{SFT Stage}: A 0.6M-parameter MLP scorer is distilled from layer-12 LLM attention via MSE regression, providing a calibrated initialization with cross-frame absolute scoring.
\item \textbf{RL Stage}: The scorer is fine-tuned via REINFORCE with PDMS (Planning-aware Driving Metric Score) as the reward, directly optimizing token selection for downstream driving quality.
\end{enumerate}
The RL stage enables two key advances: (1) the scorer can \emph{surpass} its attention teacher by discovering task-relevant importance patterns invisible to generic attention, and (2) combined with a global threshold $\tau$, the calibrated scores naturally \emph{emerge} scene-adaptive ratios---learning ``what to keep'' simultaneously learns ``how many to keep.''

% ¶5 Variant B: true speedup
To translate token reduction into real speedup, we introduce Variant~B: physical token removal from the KV sequence before LLM generation.
Unlike attention masking (Variant~A, used for quality measurement), Variant~B reduces actual FLOPs and achieves measurable wall-clock acceleration.

% ¶6 Results summary
On NAVSIM navtest ($N{\approx}11{,}576$):
\begin{itemize}[nosep,leftmargin=*]
\item Our SFT scorer at $r{=}0.5$ achieves $0.8920$ PDMS ($-0.69$pt vs.\ no pruning), already dominating random ($-3.52$pt), FastV ($-6.58$pt), and attention teacher ($-0.87$pt).
\item The RL-tuned scorer further improves to \todo{0.89X} PDMS ($+$\todo{Z}pt over SFT), the first demonstration that RL improves token selection in AD-VLAs.
\item $\tau$-cut adaptive pruning with calibrated scorer achieves $0.8940$ PDMS at mean keep-ratio 0.60, dominating fixed-ratio baselines.
\item Variant~B delivers \todo{X\%} wall-clock speedup with 38.3\% sequence length reduction.
\end{itemize}

% ¶7 Contributions
Our contributions are:
\begin{enumerate}[nosep,leftmargin=*]
\item The first RL-optimized token pruning framework for autonomous driving VLAs, using PDMS driving reward to directly optimize selection quality.
\item A unified adaptive pruning mechanism (scorer + $\tau$-cut) that learns ``what to keep = how many to keep'' without a separate budget controller.
\item Comprehensive experiments showing RL-tuned selection surpasses SFT-only distillation, with real wall-clock speedup via Variant~B token drop.
\end{enumerate}

% ============================================================================
\section{Related Work}
\label{sec:related}

\subsection{Vision Token Pruning for VLMs}

Vision token pruning has been extensively studied for general VLMs.
FastV~\citep{fastv2024} prunes tokens after layer-2 based on attention scores, achieving 45\% FLOPs reduction for VQA tasks.
LLaVA-PruMerge~\citep{llava_prumerge2024} combines CLS-attention pruning with key-similarity merging for 18$\times$ compression.
SparseVLM uses text-guided attention to identify important vision tokens without training.
ToMe~\citep{tome2023} merges tokens via bipartite matching in ViT.
These methods target general understanding tasks (VQA, captioning) and do not account for driving-specific importance patterns.

\subsection{Token Pruning for Autonomous Driving}

FastDriveVLA~\citep{fastdrivevla2026} trains a ReconPruner via MAE-style foreground reconstruction, requiring extra annotation (nuScenes-FG masks).
Prune2Drive~\citep{prune2drive2026} uses T-FPS for diversity-aware selection with per-view adaptive ratios.
MVPruner~\citep{mvpruner2026} allocates per-view budgets via diversity scores.
ST-Prune~\citep{stprune2026} exploits spatial-temporal redundancy without training.
All these methods use heuristic or supervised-only selection criteria, never optimizing directly for driving quality.

\subsection{RL for Token-Level Optimization}

TOP-RL~\citep{toprl2026} applies RL-based progressive pruning for general VLM tasks (VQA accuracy reward), demonstrating that RL improves over heuristic selection.
RL4EViT uses multi-agent RL for ViT classification token pruning.
VPPO-RL applies token-level RL for visual perception in multimodal reasoning.
\textbf{However, no prior work applies RL-based token selection to autonomous driving VLAs}, where the reward is trajectory quality rather than answer accuracy.
\ours{} fills this gap---using PDMS driving reward and the AD-VLA inference pipeline as the RL environment.

% ============================================================================
\section{Method}
\label{sec:method}

\subsection{Overview}

Given a driving scene with $N{=}720$ vision tokens from 3 cameras (240 tokens each) after ViT encoding, our goal is to select $K{=}\lfloor r \cdot N \rfloor$ tokens for LLM prefill that maximize trajectory quality.
\ours{} consists of:
\begin{enumerate}[nosep,leftmargin=*]
\item A lightweight \textbf{importance scorer} $f_\theta: \mathbb{R}^{2048+3} \to \mathbb{R}$ that maps each vision token's ViT embedding (concatenated with camera-id one-hot) to a scalar importance score.
\item A \textbf{two-stage training} pipeline: SFT distillation from LLM attention, followed by RL fine-tuning with PDMS reward.
\item A \textbf{selection mechanism}: either fixed top-$K$ or adaptive $\tau$-cut.
\end{enumerate}

\subsection{Token Importance Scorer Architecture}

The scorer is a compact MLP (${\sim}0.6$M parameters):
\begin{equation}
f_\theta(\mathbf{x}) = W_3 \cdot \text{GELU}(W_2 \cdot \text{GELU}(W_1 \cdot \text{LN}(\mathbf{x})))
\end{equation}
where $\mathbf{x} = [\text{Normalize}(\mathbf{v}); \text{cam\_onehot}] \in \mathbb{R}^{2051}$, with $\mathbf{v}$ being the layer-0 ViT$\to$LLM projection embedding and $W_1 \in \mathbb{R}^{256 \times 2051}$, $W_2 \in \mathbb{R}^{256 \times 256}$, $W_3 \in \mathbb{R}^{1 \times 256}$.

At inference, the scorer operates in a single additional pass: capture layer-0 embeddings during ViT forward, score all $N$ tokens ($<$1ms), select top-$K$, then run LLM on only $K$ tokens.
Scorer FLOPs are 0.013\% of total inference cost.

\subsection{Stage 1: SFT Distillation}
\label{sec:sft}

We initialize the scorer by distilling from the frozen VLM's own layer-12 attention signal (the layer empirically most predictive of driving-relevant importance; Section~\ref{sec:exp_ablation}).

\textbf{Teacher signal}: For each scene, we extract the attention weights at layer 12 with query = last instruction token, key = vision tokens, averaged across all heads.
This produces a 720-dimensional importance vector per scene.

\textbf{Training}: MSE regression with global z-score normalization ensures scores are calibrated across frames (essential for $\tau$-cut):
\begin{equation}
\mathcal{L}_\text{SFT} = \frac{1}{N} \sum_{i=1}^{N} \left( f_\theta(\mathbf{x}_i) - \hat{a}_i \right)^2
\end{equation}
where $\hat{a}_i$ is the globally-normalized attention score.
Training uses 4,000 scenes from navtrain, completing in $<$30 seconds on 1 GPU.

\subsection{Stage 2: RL Fine-Tuning (REINFORCE with PDMS Reward)}
\label{sec:rl}

The SFT scorer can only approximate the attention teacher---it cannot discover importance patterns invisible to generic attention.
We fine-tune it with REINFORCE, using the downstream driving metric as reward.

\textbf{Reward}: For a given token selection, we run the frozen VLM under that selection (via attention masking or token drop) and compute the PDMS score from the generated trajectory.
PDMS is a composite metric encompassing collision avoidance, drivable area compliance, ego progress, time-to-collision, comfort, and direction compliance.

\textbf{Policy}: We treat the scorer outputs as logits of a selection policy.
For top-$K$ selection, the log-probability under the multi-label softmax approximation is:
\begin{equation}
\log p_\theta(\mathcal{S} | \mathbf{x}) = \frac{1}{K}\left(\sum_{i \in \mathcal{S}} f_\theta(\mathbf{x}_i) - K \cdot \text{logsumexp}_{j=1}^N f_\theta(\mathbf{x}_j)\right)
\end{equation}
where $\mathcal{S}$ is the selected token set of size $K$.

\textbf{Advantage}: We collect rewards from a group of $G$ scenes and normalize:
\begin{equation}
A_i = \frac{R_i - \bar{R}_G}{\sigma_G + \epsilon}
\end{equation}

\textbf{Policy gradient}:
\begin{equation}
\nabla_\theta \mathcal{L}_\text{RL} = -\frac{1}{G}\sum_{i=1}^{G} A_i \cdot \nabla_\theta \log p_\theta(\mathcal{S}_i | \mathbf{x}_i)
\end{equation}

\textbf{KL regularization}: Weight-space L2 penalty to the SFT initialization prevents catastrophic forgetting:
\begin{equation}
\mathcal{L}_\text{KL} = \beta \|\theta - \theta_\text{SFT}\|_2^2
\end{equation}

The total loss is $\mathcal{L} = \mathcal{L}_\text{RL} + \mathcal{L}_\text{KL}$.
Training uses the frozen VLM as the ``environment'': each scorer update requires only VLM inference (no backward through VLM), making it computationally efficient.

\subsection{Adaptive Pruning via $\tau$-Cut}
\label{sec:taucut}

A key insight: because our SFT stage uses globally-calibrated regression (not listwise ranking), the scorer outputs have absolute cross-frame meaning.
A single global threshold $\tau$ applied to these scores simultaneously decides:
\begin{itemize}[nosep,leftmargin=*]
\item \textbf{Which} tokens to keep: those with $f_\theta(\mathbf{x}_i) > \tau$.
\item \textbf{How many} to keep: the count naturally varies per scene.
\end{itemize}
This unified mechanism eliminates the need for a separate budget controller---learning ``what to keep'' implicitly learns ``how many to keep.''
After RL fine-tuning, the scores are further calibrated to the driving task, improving $\tau$-cut performance.

\subsection{Variant B: True Token Drop for Real Speedup}

Standard attention masking (Variant~A) maintains full sequence length but zeros out pruned tokens' attention contributions---useful for quality measurement but providing no actual speedup.
\textbf{Variant~B} physically removes pruned tokens from the KV sequence before LLM generation, reducing the actual sequence from 941 to ${\sim}581$ tokens at $r{=}0.5$ (38.3\% reduction).
This translates to measurable wall-clock speedup via reduced prefill compute and shorter KV cache.

% ============================================================================
\section{Experiments}
\label{sec:exp}

\subsection{Setup}

\textbf{Backbone}: AutoVLA~\citep{autovla2025} (Qwen2.5-VL-3B), post-GRPO checkpoint (PDMS = 0.899 no-prune).

\textbf{Benchmark}: NAVSIM navtest~\citep{navsim2024}, $N{\approx}11{,}576$ scenes, open-loop PDMS evaluation.
Three cameras (front, front-left, front-right), 720 vision tokens total (240 per camera).

\textbf{Scorer training}: SFT on 4,000 navtrain scenes; RL on full navtest (11,596 scenes).
SFT: MSE loss, lr=$3{\times}10^{-4}$, 20 epochs, early stopping.
RL: REINFORCE, lr=$3{\times}10^{-5}$, group size 8, $\beta_\text{KL}{=}0.01$.

\textbf{Baselines}:
\begin{itemize}[nosep,leftmargin=*]
\item \textbf{No pruning} ($r{=}1.0$): full 720 tokens.
\item \textbf{Random}: fixed-seed random selection.
\item \textbf{Attention L12}: layer-12 attention teacher (our SFT target).
\item \textbf{FastV}~\citep{fastv2024}: layer-2 internal attention pruning.
\item \textbf{SFT Scorer} (Stage-1 only): our method without RL.
\item \textbf{SparseVLM}: text-guided training-free selection.
\item \textbf{ToMe}~\citep{tome2023}: token merging baseline.
\end{itemize}

\subsection{Main Results}

\begin{table}[t]
\centering
\caption{Main results on NAVSIM navtest ($N{\approx}11{,}576$). All at keep-ratio $r{=}0.5$ unless noted.
\ours{} (RL scorer) achieves the best PDMS among all pruning methods.}
\label{tab:main}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{$r$} & \textbf{PDMS} & \textbf{$\Delta$} & \textbf{Speedup} \\
\midrule
No pruning & 1.0 & 0.8988 & --- & 1.00$\times$ \\
\midrule
Random & 0.5 & 0.8635 & $-3.52$ & --- \\
FastV L2 & 0.5 & 0.8330 & $-6.58$ & --- \\
Attention L12 & 0.5 & 0.8901 & $-0.87$ & --- \\
SparseVLM & 0.5 & \todo{TBD} & \todo{TBD} & --- \\
ToMe & 0.5 & \todo{TBD} & \todo{TBD} & --- \\
\midrule
SFT Scorer (Ours Stage-1) & 0.5 & 0.8920 & $-0.69$ & --- \\
\textbf{\ours{} (Ours, RL)} & \textbf{0.5} & \textbf{\todo{0.89X}} & \textbf{\todo{+Z}} & --- \\
\midrule
SFT Scorer + $\tau$-cut & 0.60$^\dagger$ & 0.8940 & $-0.48$ & --- \\
\textbf{\ours{} + $\tau$-cut} & \textbf{\todo{0.6X}$^\dagger$} & \textbf{\todo{0.89X}} & \textbf{\todo{TBD}} & --- \\
\midrule
\multicolumn{5}{l}{\emph{With Variant B (real speedup):}} \\
SFT Scorer (Var.\ B) & 0.5 & \todo{0.87X}$^*$ & \todo{TBD} & 1.15$\times$ \\
\textbf{\ours{} (Var.\ B)} & \textbf{0.5} & \textbf{\todo{TBD}} & \textbf{\todo{TBD}} & \textbf{1.15$\times$} \\
\bottomrule
\end{tabular}
\vspace{-2mm}
{\scriptsize $^\dagger$Mean keep-ratio. $^*$Includes ${\sim}2\%$ decode bug; expected to match Var.\ A after fix.}
\end{table}

\subsection{Pareto Analysis}

\begin{table}[t]
\centering
\caption{Pareto curve: PDMS vs.\ keep-ratio for \ours{}.}
\label{tab:pareto}
\small
\begin{tabular}{lccc}
\toprule
\textbf{Keep-ratio} & \textbf{PDMS} & \textbf{FLOPs Saving} & \textbf{Seq.\ Reduction} \\
\midrule
$r=1.0$ (no prune) & 0.8988 & 0\% & 0\% \\
$r=0.75$ & 0.8983 & 16.9\% & 18.8\% \\
$r=0.50$ & \todo{0.89X} & 33.6\% & 38.3\% \\
$r=0.25$ & 0.8508 & 49.9\% & 56.3\% \\
$\tau$-cut (adaptive) & \todo{0.89X} & \todo{TBD} & \todo{TBD} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablation Studies}
\label{sec:exp_ablation}

\textbf{(A) RL vs.\ SFT-only}:
The RL-tuned scorer outperforms SFT by \todo{+Z} PDMS at $r{=}0.5$, confirming that driving reward provides signal beyond attention distillation.

\textbf{(B) MSE vs.\ LambdaRank distillation}:
MSE (pointwise) enables calibrated $\tau$-cut; LambdaRank (listwise) achieves slightly higher pairwise accuracy but lacks cross-frame calibration.
For the RL pipeline, MSE initialization is preferred as it enables both fixed-$r$ and adaptive modes.

\textbf{(C) Attention layer choice}:
Layer-12 attention is the optimal teacher (Section~\ref{sec:sft}), identified via 14-layer sweep.
Layer-2 (FastV) and layer-27 (last) are suboptimal.

\textbf{(D) RL hyperparameters}:
\todo{Group size, learning rate, KL coefficient sensitivity.}

\subsection{Efficiency Analysis}

\begin{table}[t]
\centering
\caption{Computational analysis at $r{=}0.5$.}
\label{tab:efficiency}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Component} & \textbf{FLOPs (G)} & \textbf{\% of Total} \\
\midrule
ViT encoder (shared) & 4.2 & 15.3\% \\
Scorer MLP & 0.004 & 0.013\% \\
LLM prefill ($r{=}0.5$) & 12.8 & 46.7\% \\
LLM decode (10 steps) & 10.4 & 37.9\% \\
\midrule
\textbf{Total} ($r{=}0.5$) & 27.4 & --- \\
\textbf{Total} ($r{=}1.0$) & 41.3 & --- \\
\textbf{Saving} & 13.9 & \textbf{33.6\%} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Wall-clock}: Variant~B achieves ${\sim}$15\% end-to-end speedup in the 2-pass configuration (pass-1 for scoring still processes full sequence).
In a future 1-pass deployment (scorer integrated into ViT), theoretical speedup reaches 38\%.

% ============================================================================
\section{Discussion}
\label{sec:discussion}

\textbf{Why RL helps}: The SFT scorer can only reproduce L12 attention patterns---it treats all scenes uniformly.
The RL stage specializes the scorer for \emph{driving-critical} importance: tokens containing lane boundaries, nearby vehicles, or traffic signals receive higher RL-boosted scores than generic ``textured'' tokens that attract attention but don't affect trajectory quality.

\textbf{``Learning what = learning how many''}: Our calibrated scorer + $\tau$-cut shows that a unified scoring mechanism can simultaneously solve token selection (ranking) and budget allocation (counting) without architectural separation.
This contrasts with prior work~\citep{prune2drive2026,mvpruner2026} that treats these as separate problems requiring separate modules.

\textbf{Limitations}:
(1) Single benchmark (NAVSIM open-loop); closed-loop evaluation is future work.
(2) Variant~B has a ${\sim}$2\% scene failure rate due to positional encoding edge cases; fixing this is engineering rather than methodological.
(3) The RL reward requires VLM inference per scene (4s/scene on H20), making training slower than pure SFT---but this is a one-time cost.
(4) Not tested on models larger than 3B parameters.

% ============================================================================
\section{Conclusion}
\label{sec:conclusion}

We present \ours{}, the first RL-optimized vision token pruning framework for autonomous driving VLAs.
By training a lightweight importance scorer with REINFORCE and PDMS driving reward, \ours{} discovers task-optimal token selection that supervised distillation alone cannot achieve.
Combined with calibrated global thresholding ($\tau$-cut), our unified approach simultaneously learns what to keep and how many to keep, eliminating the need for explicit budget controllers.
Experiments on NAVSIM demonstrate that RL-tuned selection surpasses all heuristic and SFT-only baselines, with Variant~B delivering real wall-clock speedup.
\ours{} establishes that driving-specific reward is a powerful signal for optimizing the vision$\to$language interface in autonomous driving.

% ============================================================================
\bibliography{references}

\end{document}
