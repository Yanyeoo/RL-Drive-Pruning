# 2026-06-25 (周四) Daily Journal

> 按时间点合并 3 篇当日 topic 文件 — 原文未改动，仅加入顶层 TOC + 时间索引。
>
> 主线：白天 14:31–17:47 用 4× H20 完成 M1.b₂ Stage 3（19,225 token × `(28,16,720)` per-head attention dump）→ 18:23–18:25 跑 per-scene rank-variance 分析（CPU 116 s 完）→ 18:25 起把 3 个发现 patch 回 Phase 2 design doc → 晚段对接 B1 navtrain free-lunch sweep / B1.0 metric cache build。

## 时间索引

| 时段 | 事件 | 状态 |
|---|---|---|
| 10:00 | 窗口开放，AI onboard，读 handoff + PATHS + 昨晚 journal | — |
| ~10:30 | 与用户对齐方案 1（先 dryrun → 4-GPU shard 全量） | — |
| 14:18 | Step A dryrun 启动（GPU 0, 20 token） | — |
| 14:27 | **Dryrun PASS**：20/20 OK, 2.88 s/scene, shape `(28,16,720)`, 30 GB GPU peak | ✅ |
| 14:31 | Step B 4-GPU 全量启动（`--shard-stride 4 --shard-index k`, k=0..3） | — |
| 14:44 | 4 shard 全部进入 forward 阶段 | — |
| 15:15 (T+30m) | 16% 进度, 2.40 s/scene, 0 err | — |
| 16:16 (T+90m) | 50% 进度, 2.31 s/scene, 2 err（首发 trajectory assert） | — |
| **17:47 (T+196m)** | **Stage 3 DONE**：19,217 OK, 8 err, 19,225 .pt, 24 GB, 3h16m wall | ✅ |
| 18:00 | Stage 3 落档（journal / key_results §7 / RESUME / denylist） | ✅ |
| 18:23 → 18:25 | Per-scene rank-variance 分析（16 CPU worker, 116 s wall, 0 GPU） | ✅ |
| 18:25 → ~19:00 | 把 3 个新发现 patch 回 `m1b2_phase2_design_2026-06-25.md`（§3.3 / §3.4 / G6 / R1 / Changelog） | ✅ |
| 晚段 | B1 阻塞拆解：发现 navtrain_metric_cache 不存在 → 选「选项 2」今晚只做 B1.0 | 📝 |
| 20:30+ | 整理 `docs/journal/` 目录，按 YYYY-MM-DD.md 重排（本文件为产物之一） | ✅ |
| 21:10 → 21:16 | 用户授权 B1.0 + C1 + C2 串行执行（今晚 24:00 前） | — |
| 21:16 → 21:20 | B1.0 侦察：复用 `scene_filter/navtrain_avail19k.yaml`；新建 `train_test_split/navtrain_avail19k.yaml`（4 行 wrapper）；fork `scripts/run_b1_0_navtrain_metric_caching.sh` | ✅ |
| 21:20 | B1.0 smoke (timeout 90s) PASS — hydra 接受、16 worker process pool、log loading 0% → 18% | ✅ |
| 21:20:33 | **B1.0 full run 启动** — PID 65224，16 worker，输出 `data/navtrain_metric_cache/` | ✅ |
| 21:42 | B1.0 health check：进度 ~30–50%（各 worker 不同），cache count=301, size=1.8G，无错误 | ✅ |
| **22:54:43** | **B1.0 DONE** — `Completed dataset caching! All 19225 features and targets were cached successfully.`，wall ≈ **94 min**，1839 scenario / **5.5 GB**（落盘 `metric_cache.pkl` 未压缩，故未按设计稿的 `.pkl.xz` 落盘） | ✅ |
| 22:54:44 | Metadata csv 落档 `data/navtrain_metric_cache/metadata/navtrain_metric_cache_metadata_node_0.csv`（2.8 MB） | ✅ |
| 23:30 → 24:00 | **C2 V4 spec doc DRAFT 写完**（`docs/_internal/m1b2_v4_spec_2026-06-25.md`, 149 行, `L12:{h13} + L24:{h7,h9,h10}` rank-variance 加持） | 📝 DRAFT 就位（待 T4 转正） |
| 24:00 前 | C1 V0 dryrun 脚本草稿就位（`scripts/run_c1_v0_dryrun_navtrain.sh`），**未实际运行**，留至 2026-06-26 T3 | 📝 脚本就位 |

## 🔥 晚段进度（21:10 → 现在）— B1.0 启动

### 用户决策（21:10）
今晚串行执行 **B1.0 → C1 → C2**，C1/C2 已被授权。

### B1.0 启动细节（21:16 → 21:20）

| 项 | 值 |
|---|---|
| Train_test_split wrapper | `code/third_party/AutoVLA/navsim/navsim/planning/script/config/common/train_test_split/navtrain_avail19k.yaml` (4 行, wrap 已有 scene_filter) |
| 启动脚本 | `scripts/run_b1_0_navtrain_metric_caching.sh` (镜像 `run_ma2_2_navtest_metric_caching.sh`) |
| 启动命令 | `python navsim/planning/script/run_metric_caching.py train_test_split=navtrain_avail19k cache.cache_path=.../navtrain_metric_cache worker=single_machine_thread_pool worker.max_workers=16 worker.use_process_pool=true` |
| 主进程 PID | **65224** |
| 日志 | `logs/b1_0/full_20260625_212033.log` |
| Smoke 验证 | timeout 90s 内：hydra config OK / 16 worker pool up / 1192 nuPlan log 开始 loading 0%→18% / 无 import error / 无 OOM |

### B1.0 状态轮询（21:42）
- 4 个 python 进程存活（主 65224 + 子 65702/65703 + ...）
- log 中 `Processing scenario X / Y` 各 worker（thread_id 不同），进度 30–50%
- `data/navtrain_metric_cache/` 已写出 301 个子目录，1.8 GB
- ETA：navtest 基线 6,962 token / 30 min → 等比例外推 19,225 token ≈ **83 min** → **~22:45 完成**

### B1.0 acceptance gates（22:54 实测）
- [x] `find data/navtrain_metric_cache -name metric_cache.pkl | wc -l` = **19,225 / 19,225 精确命中** ✅ （注：实际落盘文件是 `metric_cache.pkl` 未压缩，不是设计稿假设的 `.pkl.xz`；本地不阻塞，但 C1 pre-flight 与 acceptance 文档已据此修正）
- [x] 总大小 **5.5 GB**（比设计稿预测的 15–25 GB 小，因未压缩 + AutoVLA navtrain 实际场景结构更瘦；非 fail 项） ✅
- [x] log 末尾明确 `Completed dataset caching! All 19225 features and targets were cached successfully.`，无 stack trace，failed = 0 ✅
- [x] 1839 个 scenario 子目录（19,225 个 `metric_cache.pkl`，每 scenario 平均 10 个 token），metadata csv 落档 ✅

### B1.0 实际耗时（22:54 收工实测）
- Wall：21:20:33 → 22:54:44 = **94 min**（vs 设计稿预测 83 min，慢 13%，仍在合理范围）
- Throughput：19,225 token / 94 min ≈ **204 token/min**，是 navtest 单线程基线 232 token/min 的 ~88%

## 关键发现 / 产出

1. **M1.b₂ Stage 3 输入张量就绪**：`exp/m1b2_navtrain_full_alllayers/*.pt`，19,225 个 fp32 `(28, 16, 720)` tensor，每文件 1.30 MB，总 24 GB，0 OOM，error rate 0.042% (8/19225)。
2. **8 个 trajectory-assert token 是 navsim 数据噪声**：attention `.pt` 在 forward hook 内保存（assert 之前），所以 `.pt` 也已存盘；denylist 在 `_stage3_trajectory_err_tokens.txt`。
3. **rank-variance 三大发现**：
   - V1 `L12:{h13}` 在 navtrain 100% 验证（bot-K freq = 100.00%）→ M1.b₁ free-lunch 是**每场景结构性的**。
   - V3 `L24` 11-head mask **在 navtrain 上不全是 dead head**：只 3 个（h7, h9, h10）bot-4 freq > 90%。预测 navtrain PDMS 可能现 cliff。
   - Phase 2 高 headroom 层：L7 / L9 / L12 / L13 / L20 / L21（top-1 entropy 1.6–2.1 bits，41–52% of max）。
4. **rank-variance 跑得比预期快**：设计估计 30 min，实测 16 worker IO-bound 116 s。
5. **B1 阻塞**：navtrain free-lunch sweep 需要 `data/navtrain_metric_cache/`，不存在。拆解为 B1.0/B1.1/B1.2/B1.3，选项 2 = 今晚只做 B1.0。
6. **`navtrain_avail19k.yaml` 复核**：已存在（M0 自动生成），把 B1.0 造 yaml 工作从 30 min 砍到 1 min。

---

## 章节目录

1. [M1.b₂ Stage 3 — DONE (navtrain full multilayer attention dump)](#section-1)
2. [全日进度报告 — M1.b₂ Stage 3 落地](#section-2)
3. [Phase 2 prior: per-scene rank-variance analysis (DONE)](#section-3)

---

<a id="section-1"></a>

## 1. M1.b₂ Stage 3 — DONE (navtrain full multilayer attention dump)

> 源文件：`docs/journal/2026-06-25_m1b2_stage3_done.md`
> **Date**: 2026-06-25
> **Window**: 10:00 → 18:00 (effective compute 14:31 → 17:47, **~3h16m wall**)
> **Hardware**: 4× H20 (single node, ranks 0/1/2/3)
> **Status**: ✅ **COMPLETE**

### 1.0 TL;DR

Dumped per-layer × per-head vision attention tensors (shape `(28, 16, 720)`) for **all 19,225 navtrain_avail19k tokens** in **3h16m** on 4× H20, **2.25 s/scene avg**, **0 GPU OOM**, **8 trajectory-pose assertions** (data-side noise, attention .pt **still saved** before assert).

**Output**: `exp/m1b2_navtrain_full_alllayers/*.pt` — 19,225 files, 24 GB, 100% coverage of the navtrain_avail19k token set.

This is the **input for M1.b₂ Phase 2** (learned per-scene head gating policy) and for future navtrain free-lunch / Pareto sweeps.

### 1.1 Setup

#### 1.1.1 Inputs (locked from Stage 1 / 2)

| Item | Value | Source |
|---|---|---|
| Token list | `exp/m1a_navtrain_probeA_setup/navtrain_window_clean_tokens.txt` | M1.a Step 5 (locked 2026-06-24) |
| `\|tokens\|` | **19,225** | Identical to `exp/m1b2_navtrain_full_window_tokens.txt` (`diff` empty) |
| Pretokenized JSON | `data/navtrain_nocot/*.json` (19,225 files) | Stage 2 (2026-06-24) |
| Scene filter | `navtrain_avail19k.yaml` | Placeholder — bypassed because `--token-list` short-circuits scene_filter (run_attention_probe.py L159–166) |
| Model | AutoVLA Post-RFT (same as M1.a, M1.b₀, M1.b₁) | `models/...` (see `docs/PATHS.md`) |

#### 1.1.2 Probe code

- Wrapper: `scripts/run_m1a_attention_probe.sh` (transparent passthrough)
- Python: `rldrive.scoring.run_attention_probe` (multilayer-aware since Stage 1)
- Flags: `--all-layers --num-layers 28 --shard-stride 4 --shard-index k`
- Per-head: **enabled** (`average_heads=False`, shape `(layer, head, vision_pos)`)

#### 1.1.3 Sharding strategy

Used built-in `--shard-stride 4 --shard-index k` (k ∈ {0,1,2,3}). Each GPU's slice:

| Shard | num_tokens | GPU |
|---|---:|---|
| 0 | 4807 | cuda:0 |
| 1 | 4806 | cuda:1 |
| 2 | 4806 | cuda:2 |
| 3 | 4806 | cuda:3 |
| **Σ** | **19,225** | |

### 1.2 Dryrun (Step A)

| metric | observed | expected | verdict |
|---|---|---|---|
| Tokens processed | 20 / 20 | 20 | ✅ |
| Errors | 0 | 0 | ✅ |
| s/scene (steady) | 2.50 | ~2.55 | ✅ slightly faster |
| s/scene (avg incl. cold) | 2.88 | <4.0 | ✅ |
| Model load | 130.7 s | one-shot | ✅ |
| GPU memory peak | 30 GB | <50 GB | ✅ |
| `per_layer_vision_attn` shape | `(28, 16, 720)` | `(28, 16, 720)` | ✅ |

**Decision**: PASS → launched Step B immediately.

### 1.3 Full run (Step B)

#### 1.3.1 Launch command

```bash
cd /apdcephfs/private_shayladeng/tokenrl_autoVLA
mkdir -p exp/m1b2_navtrain_full_alllayers logs/m1b2_full
for k in 0 1 2 3; do
  nohup bash scripts/run_m1a_attention_probe.sh \
    --scene-filter navtrain_avail19k \
    --json-dir /apdcephfs/private_shayladeng/tokenrl_autoVLA/data/navtrain_nocot \
    --token-list /apdcephfs/private_shayladeng/tokenrl_autoVLA/exp/m1a_navtrain_probeA_setup/navtrain_window_clean_tokens.txt \
    --save-dir /apdcephfs/private_shayladeng/tokenrl_autoVLA/exp/m1b2_navtrain_full_alllayers \
    --all-layers --num-layers 28 \
    --gpu ${k} --shard-stride 4 --shard-index ${k} \
    > logs/m1b2_full/shard_${k}.log 2>&1 &
done
```

PIDs: 9189 / 9190 / 9191 / 9192.

#### 1.3.2 Watch timeline

| T+ | time | progress | s/scene | pt count | GPU MB | errors |
|---|---|---|---:|---:|---:|---:|
| 0 | 14:31 | model load | — | 0 | 0 → 30k | 0 |
| +13m | 14:44 | forward starts | 4.85 (cold) | 52 | 30887 | 0 |
| +30m | 15:15 | 770/4806 (16%) | 2.40 | 3117 | 30887 | 0 |
| +60m | 15:45 | 1555/4806 (32%) | 2.36 | 6261 | 30887 | 0 |
| +90m | 16:16 | 2380/4806 (50%) | 2.31 | 9567 | 30887 | 2 |
| +140m | 16:52 | 3435/4806 (72%) | 2.27 | 13770 | 30887 | 3 |
| +170m | 17:21 | 4180/4806 (87%) | 2.25 | (n/a) | 30887 | 6 |
| +196m | 17:47 | **DONE** | **2.25** | **19,225** | freed | **8** |

#### 1.3.3 Final per-shard summary

| Shard | OK | Skip | Err | Wall | s/scene |
|---|---:|---:|---:|---:|---:|
| 0 | 4806 | 0 | 1 | 180.9 min | 2.26 |
| 1 | 4801 | 0 | 5 | 180.9 min | 2.26 |
| 2 | 4806 | 0 | 0 | 177.5 min | 2.22 |
| 3 | 4804 | 0 | 2 | 180.5 min | 2.25 |
| **Σ** | **19,217** | **0** | **8** | **~181 min** | **~2.25** |

Wall imbalance: 3.4 min (stride sharding 自然均衡)。

### 1.4 Output verification

#### 1.4.1 File census

```
$ ls exp/m1b2_navtrain_full_alllayers/*.pt | wc -l
19225
$ du -sh exp/m1b2_navtrain_full_alllayers
24G
```

**19,225 / 19,225 .pt files = 100% coverage.** Disk: 24 GB.

#### 1.4.2 Acceptance gates

| gate | target | observed | verdict |
|---|---|---|---|
| `\|.pt\|` ≈ \|token-list\| | 19,225 ± 10 | **19,225** | ✅ exact |
| All sampled .pt shape | `(28, 16, 720)` | all pass | ✅ |
| `multi_layer=True`, `num_layers=28` | true | true on all | ✅ |
| s/scene avg | ≤ 4.0 | **2.25** | ✅ |
| GPU memory peak | < 50 GB / GPU | 30.9 GB | ✅ |
| Disk usage | < 40 GB | 24 GB | ✅ |
| Wall within 14h window | < 14h | 3h16m | ✅ |
| Error rate | < 0.1% | 8 / 19,225 = 0.042% | ✅ |

**All acceptance gates PASS.**

### 1.5 Errors (8 trajectory-pose AssertionErrors)

Attention `.pt` 在 forward hook 内保存（assert 之前），所以 8 个 err token 的 `.pt` 也已存盘（`.pt` count = 19,225, 不是 19,217）。

Affected tokens (in `_stage3_trajectory_err_tokens.txt`):

```
8d2dd1aea23a5183  67e64fb0e9245ccc  7c7cc0871be859d9  ca8281be07935921
e97dbf85c52d56b9  eecdb97c332f550d  f69d0668f4b8595e  fcad5dfb8da65554
```

Downstream policy:
- M1.b₂ Phase 2 训练 → 用全 19,225
- 未来 navtrain PDMS 评估 → 减掉这 8 个，用 19,217

### 1.6 What this unlocks

- **M1.b₂ Phase 2** — learned per-scene head gating policy training，input `(N=19225, L=28, H=16, V=720)`。
- **Per-scene rank-variance 分析**（见本日 §3）。
- **Future navtrain-side free-lunch sweep**（mirror of M1.b₁ Phase F on navtest）。

### 1.7 Artifacts

| Path | Content |
|---|---|
| `exp/m1b2_navtrain_full_alllayers/*.pt` | 19,225 per-token tensors, each `dict` with `per_layer_vision_attn (28,16,720)`, `layer_idxs [28]`, `vision_token_positions (720,)`, `last_instr_idx int`, `prompt_len int`, `multi_layer=True`, `average_heads=False`, `vision_blocks list[3]` |
| `exp/m1b2_navtrain_full_alllayers/_stage3_trajectory_err_tokens.txt` | 8-token denylist for downstream PDMS use |
| `logs/m1b2_full/dryrun.log` | Step A dryrun log |
| `logs/m1b2_full/shard_{0,1,2,3}.log` | Step B per-shard logs |
| `exp/m1b2_stage3_dryrun/*.pt` | 20 dryrun tensors |

### 1.8 Cross-references

- Stage 1 + 2 prerequisite: `docs/journal/2026-06-24.md` §3
- Pre-flight plan: `docs/_internal/plan_2026-06-24_2055_path3_execution.md`
- M1.a L\*=12 lock: `docs/journal/2026-06-24.md` §2
- M1.b₁ Phase F free-lunch (navtest): `docs/results/key_results.md` §6

---

<a id="section-2"></a>

## 2. 全日进度报告 — M1.b₂ Stage 3 落地

> 源文件：`docs/journal/2026-06-25_progress_report.md`
> **窗口**：10:00 → 24:00（GPU 4× H20 单机）
> **实际有效使用**：14:31 → 17:47（计算）+ ~30 min 落档
> **窗口利用率**：~3.5 h / 14 h ≈ **25%**（剩余 10.5 h 富余）

### 2.0 TL;DR

**一句话**：把昨晚 Stage 2 pretokenize 出来的 19,225 个 navtrain token，今天用 4× H20 全部跑出 per-layer × per-head vision-attention 张量，落盘 24 GB，**0 OOM**、**0 死锁**、**8 个数据噪声 assert（attention 已保存，无伤）**，3h16m 收工。

**核心产出**：`exp/m1b2_navtrain_full_alllayers/*.pt` — 19,225 个 `(28, 16, 720)` fp32 张量。

### 2.1 时间线（按事件）

| 时刻 | 事件 |
|---|---|
| 10:00 | 窗口开放，AI onboard |
| ~10:30 | 与用户对齐方案 1 |
| 14:18 | Step A dryrun 启动 |
| 14:24 | Dryrun model load 完成 (130.7 s) |
| 14:27 | Dryrun PASS |
| 14:31 | Step B 4-GPU 全量启动 |
| 14:44 | 4 shard 全部进入 forward |
| 15:15 (T+30m) | 16% 进度, 2.40 s/scene, 0 err |
| 15:45 (T+60m) | 32% 进度, 2.36 s/scene, 0 err |
| 16:16 (T+90m) | 50% 进度, 2.31 s/scene, 2 err |
| 16:52 (T+140m) | 72% 进度, 2.27 s/scene, 3 err |
| 17:21 (T+170m) | 87% 进度, 2.25 s/scene, 6 err |
| **17:47 (T+196m)** | **DONE — 19,217 OK, 8 err** |
| 17:50 | Acceptance + 落档完成 |

### 2.2 干了哪些工作

#### 2.2.1 计算工作（GPU）

| 阶段 | 资源 | 时长 |
|---|---|---|
| Dryrun | 1× H20 | 8 min |
| Full run | 4× H20 并发 | 3h16m wall |

每 30 min 一次 health watch，共 7 次 check。

#### 2.2.2 验收工作（CPU）

- File census：19,225（精确等于 token-list）
- Shape 抽检：random 5 + last 3 文件全部 `(28, 16, 720)`
- Error 归因：8 个 err 全部是 `Trajectory poses and sampling have unequal number of poses`（navsim 数据噪声）

#### 2.2.3 落档工作

| 文件 | 类型 |
|---|---|
| `docs/journal/2026-06-25_m1b2_stage3_done.md` | NEW (Stage 3 journal) |
| `docs/results/key_results.md` §7 | +250 行 |
| Quick-ref / Changelog | EDIT |
| `RESUME_MONDAY.md` 顶部 | EDIT |
| `_stage3_trajectory_err_tokens.txt` | NEW (8-token denylist) |
| `docs/journal/2026-06-25_progress_report.md` | NEW (本报告) |

### 2.3 性能（量化）

| 指标 | 实测 | 预算/基线 | 评价 |
|---|---:|---:|---|
| s/scene 平均 | **2.25** | 2.55 (Stage 1 单进程) | ✅ 快 11% |
| Wall（4 GPU 并行） | **3h16m** | < 14h 窗口 | ✅ 25% 利用率 |
| Wall（单 GPU 等效） | ~12 h | — | sharding 必要 |
| GPU 显存峰值 | **30.9 GB** / 98 GB | < 50 GB | ✅ <31% |
| Shard wall 不均衡 | 3.4 min | — | ✅ 自然均衡 |
| Error 率 | **0.042%** (8/19225) | < 0.1% | ✅ |

### 2.4 解锁的下一步（按价值排序）

| # | 任务 | GPU? | 估时 | 价值 |
|---|---|---|---|---|
| 1 | **M1.b₂ Phase 2 design doc** | ❌ | 1–2 h | 高 |
| 2 | Per-scene rank-variance 分析 | 轻 | ~30 min | 中 |
| 3 | M1.b₂ Phase 2 训练原型 | ✅ | 2–6 GPU-h | 高 |
| 4 | navtrain free-lunch sweep | ✅ | ~12 GPU-h | 中 |

### 2.5 风险 / 待办

- 8 个 trajectory-assert token：denylist 已落档
- `exp/m1b2_stage3_dryrun/` 保留作 reference (25 MB)
- scene_filter `navtrain_avail19k` 是 placeholder（被 `--token-list` 短路）
- Stage 1 multilayer 代码改动尚未 merge 到 main

### 2.6 一句话总结

「**Stage 3 跑完了**，输入 `(N=19225, L=28, H=16, V=720)` 全在 `exp/m1b2_navtrain_full_alllayers/*.pt`，下一步去写 Phase 2 design doc，**不需要 GPU**。」

---

<a id="section-3"></a>

## 3. Phase 2 prior: per-scene rank-variance analysis (DONE)

> 源文件：`docs/journal/2026-06-25_m1b2_rank_variance.md`
> **Window**: 2026-06-25 18:23 → 18:25 (116 s wall, 16 CPU workers, 0 GPU)
> **Successor / target**: M1.b₂ Phase 2 — see `docs/_internal/m1b2_phase2_design_2026-06-25.md`

### 3.0 TL;DR

Computed per-(layer, head) rank statistics across all **19,225** navtrain attention tensors:

1. **Validates M1.b₁ free-lunch on navtrain at the rank level**: every V1/V2 mask head sits in bottom-4 on **100%** of scenes; the mask is structural, not navtest artifact.
2. **Reveals which layers have learning headroom for Phase 2**: 10 layers show "moderate variation" (top-1 entropy 1.2–2.1 bits = 30–52% of max), led by L21 (2.08), L20 (1.98), L12 (1.76), L13 (1.72), L9 (1.64).
3. **Reveals an M1.b₁ V3 risk** previously invisible: 6 of 11 L24 heads in V3 actually have bot-K freq < 50% on navtrain — L24 mask is *not* "uniformly dead" on navtrain, contradicting navtest's V2→V3 = −0.08 pp.

### 3.1 Inputs / outputs

| | |
|---|---|
| Script | `scripts/m1b2_rank_variance.py` (no GPU, ProcessPoolExecutor, 16 workers) |
| Inputs | 19,225 × `(28, 16, 720)` fp32 tensors |
| Outputs | `exp/m1b2_rank_variance/` — npz, json, md, 3 heatmaps |
| Wall | **116 s** (vs. design estimate of 30 min) |
| Errors | 0/19,225 |

Score: `score(l, h, s) = mean over V of A(s)[l, h, :]`. Rank per layer = argsort ascending in [0, H-1]; 0 = lowest, 15 = top-1.

### 3.2 Key result — M1.b₁ mask validation on navtrain

For every head in V1/V2 (4 heads total), bot-K freq = **100.00%** on navtrain:

| Variant | Layer | Head | rank_mean | rank_std | bot-K freq | Reading |
|---|---:|---:|---:|---:|---:|---|
| V1 | 12 | 13 | 0.00/15 | 0.00 | 100.00% | always rank 0 |
| V2 | 27 | 0 | 2.00/15 | 0.06 | 100.00% | rank 2 on every scene |
| V2 | 27 | 8 | 0.00/15 | 0.00 | 100.00% | always rank 0 (numerical zero) |
| V2 | 27 | 9 | 1.00/15 | 0.06 | 100.00% | rank 1 on every scene |

**Implication**: V1 is provably optimal as static mask on navtrain — Phase 2 cannot improve L12:{h13} without expanding beyond bot-K.

### 3.3 New finding — V3 L24 mask is heterogeneous on navtrain

| Layer 24 head | bot-4 freq | Interpretation |
|---:|---:|---|
| 1 | 0.03% | Almost **never** weak |
| 2 | 0.00% | **Never** weak (rank_mean 9.83) |
| 6 | 13.04% | Mostly **not** weak |
| 8 | 0.00% | **Never** weak (rank_mean 8.96) |
| 12 | 16.60% | Mostly **not** weak |
| 14 | 4.46% | Almost never weak |
| 15 | 19.79% | Mostly **not** weak |
| 0 | 53.51% | Borderline |
| 7 | 93.67% | OK to mask |
| 9 | 99.98% | OK to mask |
| 10 | 98.92% | OK to mask |

Only 3 of 11 L24 heads (h7, h9, h10) are reliably bot-K per scene; 7 heads are above rank-4 on majority of scenes. V3 ≈ V2 on navtest is "compensation by redundancy" but predicts brittleness on navtrain.

**Implication for Phase 2**:
- V4 sub-variant `L12:{h13} + L24:{h7,h9,h10}` = **principled free-lunch L24 add-on** (3 reliably-dead heads, not all 11).
- L24 learned per-scene policy has **the most headroom** of any layer for V3 budget.

### 3.4 Per-layer headroom map (Phase 2 prior)

Top-1 entropy = entropy of head-winner distribution across 19,225 scenes. log2(16) = 4.0 = max-uniform.

| Headroom tier | Layers | Normalized entropy |
|---|---|---|
| **High** (> 40% of max) | **L7, L9, L12, L13, L20, L21** | 41–52% |
| **Moderate** (25–40%) | L3, L10, L11, L14, L18, L19, L23, L25, L27 | 25–37% |
| **Low** (10–25%) | L4, L6, L15, L17, L26 | 15–22% |
| **Static** (< 10%) | L0–L2, L5, L8, L16, L22, L24 | < 9% |

Notes:
- **L12 high-headroom + V1 still works**：variability 在 top，不在 bottom（h13 uniformly rank 0）。
- **L24 "static" by top-1 entropy** 但 bot-K 异质 — average winner consistent，但 bottom-4 set rotates。Phase 2 应学 bottom。
- **L20/L21 高 entropy + 低 M1.b₁ coverage** — biggest "blue ocean" for Phase 2。

### 3.5 What changes for the Phase 2 design doc

Patches applied to `docs/_internal/m1b2_phase2_design_2026-06-25.md`:

1. **§3.3 pseudo-labels**: "intersection of per-scene bot-K across a sliding window" 替代单点 bottom-k。
2. **§2.3 action space**: add C4 = "fixed per-layer bot-K pool, learn which to drop per scene"（L24 finding 启发）。
3. **§6 acceptance**: add stretch gate G6 = "match V1 on L12:h13 + beat V3 on L24 by < −0.08 pp at ≥ 5% KV saving"。
4. **§8 risk R1** (prompt embedding signal): mitigate by 先训 high-entropy 层 (L20, L21, L9, L13)。

### 3.6 Reproducibility

```bash
# full
$PY scripts/m1b2_rank_variance.py --workers 16 --out exp/m1b2_rank_variance

# smoke
$PY scripts/m1b2_rank_variance.py --workers 4 --limit 50 --out exp/m1b2_rank_variance_smoke
```

Inputs are deterministic .pt tensors; output is bit-identical between runs.

### 3.7 Artifacts

| Path | Size | Note |
|---|---|---|
| `exp/m1b2_rank_variance/rank_stats.npz` | 19 KB | rank_mean / rank_std / top_k_freq / bot_k_freq / score_mean / score_std / top1_hist / per_layer_entropy |
| `exp/m1b2_rank_variance/rank_variance.json` | 7 KB | Machine-readable summary + M1.b₁ probe |
| `exp/m1b2_rank_variance/SUMMARY.md` | 5 KB | Human-readable tables |
| `exp/m1b2_rank_variance/heatmap_rank_std.png` | 45 KB | (28, 16) — where is rank variable |
| `exp/m1b2_rank_variance/heatmap_bot_k_freq.png` | 45 KB | (28, 16) — where can we safely mask |
| `exp/m1b2_rank_variance/heatmap_top1_entropy.png` | 31 KB | per-layer bar chart |
| `scripts/m1b2_rank_variance.py` | source | — |
| `logs/m1b2_rank_variance/full.log` | — | run log |

### 3.8 Next

- Apply 4 design-doc patches (§3.5 above). ✅ DONE 18:25–19:00.
- Proceed to B1 (navtrain free-lunch sweep) for empirical PDMS confirmation of V3 brittleness hypothesis → tonight 选项 2: B1.0 only。
- Future Phase 2 prototype to use this analysis as feature prior.
