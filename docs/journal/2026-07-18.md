# 2026-07-18 — 20h 窗口进度核验与续跑

**Author**: agent  
**Check time**: 15:08 CST

---

## 1. 事实核验

- 当前实验进程：15:08 时无 `train_scorer_grpo.py` / `run_pdm_score_cot.py`，GPU 全空闲。
- 企业微信 heartbeat：进程仍在，但日志显示 urllib 证书错误，部分 2h 自动汇报失败。
- 已修复 heartbeat：改为 `curl` 发送，并重启，PID `110085`。

## 2. 已完成结果

### Scorer GRPO shard training（GPU0-3）

4 个 scene-shard 训练均已完成，final ckpt 时间约 09:23-09:27：

- `ckpt/s3_token_scorer_rl_20260717_230129_sh0/`
  - `train_log.jsonl`: 1089 steps
  - final running_reward ≈ 0.8749
- `ckpt/s3_token_scorer_rl_20260717_230129_sh1/`
  - 1089 steps
  - final running_reward ≈ 0.8592
- `ckpt/s3_token_scorer_rl_20260717_230129_sh2/`
  - 1089 steps
  - final running_reward ≈ 0.8522
- `ckpt/s3_token_scorer_rl_20260717_230129_sh3/`
  - 1089 steps
  - final running_reward ≈ 0.8435

### Variant B safe fallback full re-eval（GPU4-7）

4 shards 全部完成，0 failed：

| shard | N | PDMS |
|---|---:|---:|
| sh0 | 2949 | 0.8763 |
| sh1 | 2796 | 0.8696 |
| sh2 | 2963 | 0.8702 |
| sh3 | 2868 | 0.8739 |

Aggregate: **N=11576, PDMS=0.8725**.

结论：safe fallback 没有修复 Variant B 质量，full result 仍明显低于 Variant A/SFT scorer；说明 catastrophic bug 不只是 `<num_poses` 过短可检测 case，后续需要更深的 decode/M-RoPE 对齐修复。

## 3. 问题记录

### 问题 #1：09:27 训练完成后未自动续跑 eval，GPU 15:08 被发现空闲

**原因**：只启动了 heartbeat 汇报脚本，未启动实验续跑 orchestrator；heartbeat 也因证书错误未正常每 2h 发出全部汇报。

**修复**：15:09 立即启动 RL scorer r=0.5 full-navtest eval 两套候选：

- GPU0-3: `MT_rlsh0final_r05_sh{0..3}` using `ckpt/s3_token_scorer_rl_20260717_230129_sh0/`
- GPU4-7: `MT_rlsh0best_r05_sh{0..3}` using `ckpt/s3_token_scorer_rl_20260717_230129_sh0/ckpt_best`

**Reverse**：如发现 ckpt 选择不合理，停止上述 eval，改跑 4 shard ckpt 的 shard0 quick selection 后再 full eval。

## 4. 当前运行

- `logs/rl_eval_r05_latest_tag.txt`: `20260718_150951`
- 8 个 RL r=0.5 eval 进程已启动，正在模型加载阶段。

## 5. 15:15 决策：6点回收前切换到 Q1500 quick eval

**事实核验**：15:14 时 8 个 full-navtest eval 仍在模型加载/刚进 scoring；按历史速度 full 4-shard 约 3.3-3.7h，18:00 前大概率无法完成并产出 CSV。

**决策**：停止 `MT_rlsh0final/best_r05_sh*` full eval，改跑 `navtest_s2sub1500_shard0` quick eval，用 8 卡同时评测 4 个 shard ckpt 的 final/best 共 8 个候选，确保回收前至少拿到可比较的 RL scorer 候选排名。

**Reverse**：如果 GPU 回收延后或 quick eval 提前完成，选择 best candidate 后恢复 full-navtest eval。

## 6. 17:44 Q1500 RL candidate results

Q1500 quick eval finished (all 8 candidates). GPU idle after completion.

| rank | candidate | N | PDMS |
|---:|---|---:|---:|
| 1 | `rlsh3best` | 1495 | 0.889109 |
| 2 | `rlsh0best` | 1495 | 0.883768 |
| 3 | `rlsh1best` | 1495 | 0.882968 |
| 4 | `rlsh2best` | 1495 | 0.882016 |
| 5 | `rlsh0final` | 1495 | 0.873399 |
| 6 | `rlsh2final` | 1495 | 0.873136 |
| 7 | `rlsh3final` | 1494 | 0.864597 |
| 8 | `rlsh1final` | 1494 | 0.861224 |

Reference on same split from `key_results.md §10.2`: SFT scorer r=0.5 = **0.895262**. Therefore best RL candidate (`rlsh3best`) is **-0.615 pt vs SFT** on Q1500.

Interpretation:
- All `best` checkpoints > corresponding `final` checkpoints: confirms overtraining / early stopping sensitivity.
- Current Scorer GRPO does **not** beat SFT scorer; do not make RL the main paper claim yet.
- Fallback story should be SFT scorer + calibrated τ-cut adaptive; RL becomes appendix/negative ablation unless new objective/checkpointing fixes generalization.
