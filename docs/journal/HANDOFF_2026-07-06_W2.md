# HANDOFF → W2  (written 2026-07-06 16:30, W1 close)

> **You wake up with NO memory.** Read this + `docs/plan/s3_execution_plan.md` +
> `docs/results/key_results.md §10/§11` + `docs/journal/2026-07-06.md`. Rules:
> fact-check first / pgrep before launch (no double-run) / `cp -a` before key ops
> / journal any deviation + reverse / real-time todos. Env: 168 H20 card-h/week,
> ≤24h window, memory lost between windows. Project root
> `/apdcephfs/private_shayladeng/tokenrl_autoVLA`; py `miniconda3/envs/autovla/bin/python`.

## State at W1 close
- **S1 executor** ✅ lossless. **S2 gate** ✅ PASS. **S3 SFT scorer** ✅
  `ckpt/s3_token_scorer/` (test pairwise-acc 0.839); beats attn_L12 at r≤0.5,
  r=0.5 PDMS 0.8953 > r=1.0 0.8914 on the 1500-subset (§10).
- **claim② Budget Policy = NEGATIVE** (§11): adaptive never beats fixed r=0.5
  (4 configs, best −0.55pt). PDMS is deterministic ⇒ oracle gain is real but not
  learnable from low-dim scene features. **Do NOT run budget-policy GRPO blindly.**
- Direction pivoted to **claim①/③ (fixed-r learned scorer)** as the headline.

## What is RUNNING — **MIDNIGHT (00:00) RECLAIM tonight; disk persists, GPU+memory don't**
Main-table run re-prioritized so cheap-essential FULL-navtest arms finish before 00:00:
**scorer r=0.5 (~20:00) → r=1.0 ref (~22:00) → random r=0.5 (~23:55)**. attn_L12 r=0.5
and scorer r=0.25/0.75 get cut off at reclaim (killed job = no CSV = clean; SKIP_DONE
reruns next window). Mechanism: `STOP_S3` drained the initial scorer-r0.5 pass, then
`scripts/_s3_p2_sequencer.sh` relaunched `run_s3_maintable_full_navtest.sh` with env
`ARMS_SPEC` in priority order. CSVs → `results/raw/tokenprune_S3_full/MT_<sel>_r<rt>_sh<0..3>.csv`
(persistent disk). Logs `logs/_s3_maintable.log`, `logs/_s3_p2_seq.log`.
Aggregator (run anytime): `python scripts/s3_aggregate_maintable.py`.

## DECISION (user, 2026-07-06): **A** — method = dynamic token selection (scorer) + **fixed r=0.5**
claim② (adaptive budget) is a **negative result / ablation** (see design_decisions
Revision 2026-07-06 + key_results §11). Budget Policy / GRPO (Phase B/C) **dropped**.
No A/B question anymore — proceed to aggregate the main table and finalize the
fixed-r story. The continuous-threshold "emergent ratio" mechanism is NOT pursued
(user chose A); only revisit if explicitly asked.

## W2 step-by-step
1. Boot checks: `nvidia-smi`; `pgrep -af run_pdm_score_cot` (none after reclaim).
   **Resume remaining arms**: `nohup bash scripts/run_s3_maintable_full_navtest.sh
   > logs/_s3_maintable.log 2>&1 &` (default ARMS_SPEC; SKIP_DONE skips arms already
   on disk from tonight → runs attn_L12 r=0.5 + scorer r0.25/0.75; ~10h on 4 cards).
2. **Aggregate anytime**: `python scripts/s3_aggregate_maintable.py` → table +
   iso-r=0.5 comparison → `results/raw/tokenprune_S3_full/_aggregate.md`. Tonight's
   disk already has scorer r=0.5 + r=1.0 (+maybe random) → the claim① headline
   (scorer r=0.5 vs r=1.0 = ~lossless?) is computable now. Write into `key_results §12`.
   If scorer r=0.5 drops vs r=1.0 on full navtest, report honestly + note r=0.75.
3. **Pareto figure**: scorer/attn/random PDMS vs keep-ratio on full navtest.
4. **M6 remaining** (design Q5): add FastV + FastV-selector-at-input baselines;
   assemble main table (EPDMS + collision/comfort/progress subcols) + A1–A8/A10
   ablations (claim② negative = an ablation). Figures.
5. Optional low-priority: scorer online GRPO (M2) if pushing scorer past attn_L12.

## Assets / paths
- scorer ckpt `ckpt/s3_token_scorer/`; budget-policy (negative) `ckpt/s3_budget_policy{,_aug}/`.
- navtrain features (4000) `data/s3_scorer/features/`; navtest-sub1500 features (1495)
  `data/s3_scorer/features_navtest_sub1500/`.
- code: `rldrive/scoring/{token_scorer,budget_policy,run_feature_dump,attention_capture}.py`,
  `rldrive/agents/autovla_with_token_prune.py` (selector=scorer), scripts `s3_*`,
  `run_s3_maintable_full_navtest.sh`.
- backups: `backups/cycle_close_20260706_*`.

## NOT done (by design / pending user)
- Budget-policy GRPO (Phase B) — gated OFF by §11 negative.
- Scorer GRPO (M2) — optional, low priority (SFT scorer already strong).
- FastV baselines, full ablation table, figures — M6.
- 8-card windows: W1 only saw 4 cards; if 8 appear, scripts auto-scale (worker per GPU).
