# 2026-07-03 — 项目从头到尾全景总结（应 user 要求落盘）

**Author**: agent (autonomous-mode)
**用途**: 一份自洽的项目全景，供任何失忆重启 / 新 session / user 快速读完背景+现状+待决策。
**姊妹文档**: 本周期执行细节见 `2026-07-02.md`；权威数字见 `docs/results/key_results.md`；设计见 `docs/plan/design_decisions.md`。

---

## 一、项目是什么

**RL-Drive**：面向自动驾驶 VLA 的**驾驶场景条件化视觉 token 剪枝**，用 RL 学习。
- **动机**：VLA 的 FLOPs 绝大部分花在 vision token 的 LLM prefill（~70%），但很多 token 对当前驾驶决策冗余。
- **骨干**：AutoVLA（Qwen2.5-VL-3B），frozen。ckpt `models/AutoVLA/AutoVLA_PDMS_89.ckpt`。
- **Benchmark**：NAVSIM **navtest** open-loop PDMS/EPDMS（06-15 从 navhard_two_stage 切换，因 AutoVLA 上游 navsim fork 不原生支持 navhard 双路径）。
- **三条核心 claim（待验证）**：①≥50% token 节省几乎不掉点；②自适应 budget > 固定比例；③驾驶条件化 scorer ≫ 纯 vision-attention。

## 二、设计冻结（2026-06-14，Q1–Q5）

**两阶段架构**（frozen 骨干；剪枝在 ViT→LLM 接口 = external pruning）：

| 阶段 | 模块 | 目标 | 监督 |
|---|---|---|---|
| **Stage A** | Importance Scorer（轻量 MLP）| token 按驾驶相关性排序 | LLM-attention 蒸馏(LambdaRank) → GRPO(EPDMS-advantage) |
| **Stage B** | Budget Policy | 每场景选 keep 比例 {0.25/0.5/0.75/1.0} | B3 Pareto-aware oracle(SFT) + R3 piecewise Pareto reward(GRPO) |

关键决策：G4-with-camera-id（budget 跨相机自由流通）；scene-context 三件套(nav_cmd+ego_speed+driving_instr)；两阶段 separate；per-scene advantage 降方差；reward clip[-1,1]。评测：7 baselines + A1–A10 ablations，主报 navtest ratio=0.5 + Pareto 曲线。

## 三、Milestone 蓝图

`M0 baseline → M1 Scorer(SFT+RL) → M2 → M3 oracle → M4/M5 Budget Policy(SFT+RL) → M6 评测`，原估 12–15 工作日。

## 四、实际进展 vs 蓝图（诚实现状）

实际工作集中在 **M0 + M1.a/M1.b 的探索性分析**，并发生**方向转向**——完整 M2–M6 RL pipeline（scorer/budget policy 的 SFT+GRPO）**尚未搭建**。

已完成并锁定：
1. **B0 baseline（M0, LOCKED）**：navtest PDMS = **0.8983**（n=11576），vs paper 0.8911 **+0.72pt** ✅ 复现成功。瓶颈子项 = ego_progress 0.83（其余 ≥0.96）。
2. **M1.a 层探测**：L\*=**12**（last-instr→vision attention 最对齐下游质量）。
3. **M1.b₀ per-head 分解**：L12 有 1 dead head(h13)，L27 有 2(h8,h9)，ranking 样本不变(ρ=1.0)。
4. **M1.b₁ free-lunch sweep（LOCKED）**：V1=L12:{h13} 删除 Δ=−0.0004（免费）；含 L27 mask 的 V2 = cliff(−4.4pp) → **L27 是悬崖**。
5. **M1.b₂**：全量 navtrain per-head attention dump（19225 tokens）完成；但**离线 dynamic probe 天花板被自己证伪**（K_eff=4=静态 Sc4，offline surrogate 无法 Pareto dominate，真 dominate 需 online real-reward，不在 design doc）。

## 五、关键转向（06-30 → 07-01）

- **dynamic Pareto 主线（原 novelty 核心）被数据证伪** → 转向 **路 A：剪枝判据分析论文**（user 07-01 拍板）。
- 路 A 脊柱 = **"层 × 可剪性" landscape**（每层删 bot-4 head 测 ΔPDMS）。
- 路 C（online real-reward dynamic）挂 future work。

## 六、landscape 工作（06-30 → 07-03）当前完整曲线（干净 4-shard, n≈11574/点）

```
L4 +0.0007 · L8 +0.0009 · L12 +0.0004 · L16 +0.0005 · L20 −0.0022
· L22 +0.0001 · L24 −0.0027 · L25 +0.0015 · L26 −0.0004 → L27 −0.04495
```

**🔑 本周期(07-02→07-03)关键发现 —— 推翻旧假设**：
- 06-30/07-01 断言"代价随层位置**单调**上升" → 补入 L22/L25/L26 后**证伪**。
- **cliff 是"末层突墙"不是缓坡**：紧邻输出的 L26 仍 free(−0.0004)，L27 骤降 ~115× 噪声。
- 稳健结论：**bot-4 删除在整个 decoder 近乎处处 free，唯独最后一层 L27 结构性不可剪**。
- 旧 depth-Spearman=−0.943 因非单调已失效，待重算。

## 七、当前运行态（07-03 16:xx）

- 无人值守 driver/watchdog 已停（STOP_DRIVER 在）；**L0K4 补跑中**（standalone, TIMEOUT=12000, 2×H20），估计 18:00 只完成 shard0/1。
- key_results §6.8 已用完整 10 点 landscape 重写 + changelog；图 `docs/results/figures/layer_prunability_landscape.png`（11 点）；备份 `backups/cycle_start_20260702_184127/` + `backups/cycle_close_20260703_153418/`。

## 八、待决策 & 风险

- **⚠️ 唯一待 user 拍板**：framing 是否从"单调梯度"正式改为"末层局部化突墙"（建议采用，更 defensible：reviewer 难反驳"最后一层特殊"）。
- **未做**：M2–M6 全部 RL pipeline（scorer/budget policy、7 baselines、Pareto 主表）。走路 A 大部分不需要；回路 C 需从头搭 online 训练。
- **关键风险**（risks.md）：R-D-3（AutoVLA headroom 小，已部分应验——free-lunch 空间确实有限）；R-Ops-1（H20 曾被回收，watchdog 已缓解）。

## 九、一句话现状

> 项目从"两阶段 RL 动态剪枝"宏大设计，务实收敛到 **"层级可剪性判据分析"**（路 A）：已用 AutoVLA/navtest 干净证明 **bot-4 head 删除在整个 decoder 几乎免费、仅最后一层 L27 是结构性悬崖**——当前最扎实、最 defensible 的论文脊柱。下一步等 user 对 framing 拍板 + 决定是否补 K=6 墙 / 回归 online 动态路线。

## 十、环境 / 路径（照搬）

- 项目根：`/apdcephfs/private_shayladeng/tokenrl_autoVLA`（**不是** workspace 根）
- Python：`/apdcephfs/private_shayladeng/miniconda3/envs/autovla/bin/python`
- GPU：2× H20
- 续跑必读（新→旧）：本文 → `docs/results/key_results.md` §6.8 → `docs/journal/2026-07-02.md`（本周期偏离+交接）→ `docs/plan/design_decisions.md`

---

## 17:xx — 方向决策：user 拍板重启动态 token 剪枝（撤销路 A 退守）

**user mandate (17:14)**: "我还是想坚持动态剪枝并实现目标"。

**事实核验（code-explorer 全仓盘点）先行**：被 06-30/07-01 证伪的是 **L12 head-gating offline probe**（16-head 窄空间）；Q1–Q4 设计的**真正动态方法 = ViT→LLM 接口 vision token 级剪枝(N≈720 选 top-B)+Budget Policy+online GRPO**，**从未搭建、不在被证伪范围**。故原始目标仍开放。缺 4 件核心组件（token 剪枝执行器 / per-token scorer / budget policy / 接入 GRPO），但 attention 捕获 + dump + GRPO+PDMS 骨架已现成可复用。

**关键路径（S1→S2 gate→S3，先测再大建）**：
- S1（纯代码）：token 剪枝执行器，先 attention-mask 变体（无 M-RoPE position surgery，低风险），r=1.0 lossless。
- **S2（决定性 gate，GPU）**：免训练 L12-attention selector 跑 fixed-r Pareto + per-scene oracle r\*，测 headroom + scene 方差。**过了才建 S3**。
- S3：token Importance Scorer(LambdaRank，label 来自 dump)→Budget Policy→online GRPO。

**user 选择（q1/q2）**：先**只出技术方案文档**评审、不动代码；**L0K4 先跑完拿 landscape 左端点，S2 排其后**。

### 本轮产出（文档 only，无代码、无新 GPU 任务）
1. `docs/plan/design_decisions.md` **Revision 2026-07-03**：重启动态剪枝、澄清"head-gating 证伪≠token 剪枝"、定 S1/S2/S3 关键路径 + headroom gate。（Q1–Q5 主设计不变；路 A landscape 降为 ablation A5 证据；路 C 升主线。）
2. `docs/specs/dynamic_token_pruning_S1_spec.md`：执行器技术方案（Variant A attention-mask / Variant B 真丢 token；注入点复用 `PromptIndex`+hook 范式；r=1.0 lossless + A/B 忠实性验收；M-RoPE 风险）。
3. `docs/specs/dynamic_headroom_gate_S2_spec.md`：gate 实验（arms/oracle r\*/判定 PASS-PARTIAL-FAIL/GPU 计划 ~25 GPU-h/待 user 确认的 4 个开放问题）。

### 当前态 & 待办
- **未写任何代码、未起新 GPU 任务**（严格遵 user "先出文档评审"）。L0K4 仍在补跑（landscape 左端）。
- **待 user 评审 3 份文档**，尤其 S2 §6 的 4 个开放问题（r 网格 / ε / 是否加 ViT-L2-norm selector / 判 S3 的 ceiling-gain 阈值）。评审通过后再动手 S1 代码。

## 17:2x — user 评审拍板 (open questions 定案)
- r 网格 = **{0.25,0.5,0.75}** ✅
- ε = oracle 容忍阈值（S2 仅后处理，非训练）；**主 ε=0.01** + 0.005/0.02 稳健。
- 第四个 selector（FastV-at-input / L2-norm）：**gate 阶段不加**，挪到 gate PASS 后的 M6（省 GPU，fact-first）。
- S3 判定 bar：oracle ceiling gain ≥ ~0.5pt。
- 已锁进 `dynamic_headroom_gate_S2_spec.md` §6。**gate = 正好 4 GPU-run**（attn_L12 @{.25,.5,.75} + random@.5）。

## 17:28 — user "开工，六点回收" → S1 编码 (纯代码, 无 GPU)

**产出（编译通过、lint 0、CPU 单测全过）**：
- `code/rldrive/agents/token_prune_patch.py`：Variant A = 把被剪 vision token 在 **2D attention_mask 里标 padding(0)** → 经模型 `_update_causal_mask` 转 -inf，全程(prefill+decode)不可被 attend；不碰 M-RoPE。含确定性 `select_prune_positions`（top-B by score，tie 保留低位）。空剪枝 = no-op（r=1.0 lossless 路径）。
- `code/rldrive/agents/autovla_with_token_prune.py`：`AutoVLAWithTokenPruneAgent`（2-pass：pass1 抓 L12 attn 当分 → 选 top-B → pass2 在剪枝 mask 下 generate）。selector=attn_L12/random；keep_ratio=1.0 时跳过 pass1、no-op。Variant B(真丢 token) 显式 NotImplementedError 留 S3。
- `code/tests/test_token_prune_select.py`：5 个 CPU 单测（top-B / keep-all / 1/4 / tie-break / 空 no-op）**ALL PASS**。
- 不改 third_party。

**S1 未完成（需 GPU，排 L0K4 之后）**：r=1.0 lossless 复现 B0（位级一致）+ prune-effect 抽查 + Variant A vs B 忠实性 1-scene（spec §3.1–3.4）。L0K4 仍在占 2×H20（未打扰）。

**下一步**：L0K4 完（~20:30）→ 跑 S1 GPU 验收 → 过了接 S2 gate（4-shard，复用 phaseF 分发，`agent._target_=rldrive.agents.autovla_with_token_prune.AutoVLAWithTokenPruneAgent`）。
