# 2026-08-06 v6 Per-Token Counterfactual REINFORCE Handoff

> 写于 16:35。训练 step 2，auto_chain 已就位。
> GPU 明天下午 18:00 回收。

## 0. 事实核验

### 0.1 v5 最终结果（全量）

| 方法 | PDMS | vs SFT(0.89008) |
|---|---|---|
| v5 FINAL (4 shard) | **0.84850** | -0.0416 ❌ |
| v5 BEST (sh0) | 0.87667 | -0.0134 |
| v5 SAFENET (sh0) | 0.85345 | - |
| v3 旧版 (sh0) | 0.85429 | - |
| SFT r=0.355 | ❌ ckpt key不兼容 | - |
| 7B nuScenes | ❌ 图片路径全错 + metrics依赖缺失 | - |

### 0.2 退化轨迹

```
SFT r=0.5:   0.89008  ← 基线
v3:          0.87066  ← eff_beta=0.15, proxy reward
v4:          0.86094  ← eff_beta=0.05, proxy reward  
v5:          0.84850  ← True PDMS reward, 无efficiency_bonus
```

**Scene-level budget RL 天花板 ≈ 0.87。**

### 0.3 v6 代码改动

- `scripts/train_scorer_budget_rl.py`：
  - 新增 `--counterfactual-k` 参数（默认4）
  - 新增 `process_one_counterfactual()` 函数：对每个scene采样K个kept token，逐个swap out并re-evaluate PDMS
  - token_net 用 per-token advantage 更新：A_i = R_pruned - R_{-i}
  - budget_net 保持 scene-level Gaussian REINFORCE
  - 每 scene 做 2 + K = 6 次 VLA forward（baseline + pruned + K counterfactual）
- 备份：`backup/20260806_v6_launch/`

---

## 1. 运行状态

### 1.1 训练
- PIDs: 157316, 157319, 157322, 157325
- 脚本: `scripts/launch_v6_train.sh`
- ckpt: `ckpt/s3_token_scorer_budget_rl_v6_20260806_162439_sh{0..3}/`
- 日志: `logs/v6_train/train_sh{0..3}.log`
- 进度: step 2, ~97s/step (counterfactual 每步多4次VLA forward)
- 配置: counterfactual_k=4, group_size=8, 1 epoch

### 1.2 Auto-chain
- PID: 157919
- 脚本: `scripts/auto_chain_v6.sh`
- 日志: `logs/v6_train/auto_chain_*.log`

### 1.3 Step 1-2 观察
```
sh0: step1 R=2.669 kr=0.534 grad=0.000
sh0: step2 R=2.551 kr=0.562 grad=0.029
```
- R值健康（~2.5-2.7 = PDMS product ~0.83-0.89）
- grad很小是per-token方法的特性：单个token的边际PDMS贡献通常<0.001
- 需要在多步中累积学习

---

## 2. 预计时间线

```
16:24  训练启动
~22:00 1 epoch完成 (~5.5h, 4807/8=600 steps × 97s)
~22:00 v6 全量 eval (4 GPU, ~3.6h)
~01:30 Eval完成
~01:30 消融 (ckpt_best, safety_net, v3对比)
~05:00 消融完成
~05:00 7B nuScenes (best-effort)
~18:00 GPU回收
```

---

## 3. 关键监控信号

| 指标 | 健康范围 | 注意 |
|---|---|---|
| R (reward) | 2.0-3.0 | R = driving_scale × PDMS_product |
| kr | 0.45-0.60 | 太高考验budget head |
| grad_norm | 0.001-0.1 | per-token方法grad天然小，正常 |
| selection_log_prob | -15 ~ -8 | per-token softmax log prob |

**v6核心判断**：per-token advantage能否学到有意义的token选择。如果grad持续≈0说明单个token贡献太小无法区分，需要增大counterfactual_k或换方法。

---

## 4. Reverse指令

```bash
kill 157316 157319 157322 157325  # 训练
kill 157919                         # chain
rm -rf ckpt/s3_token_scorer_budget_rl_v6_20260806_162439_*/
rm -rf logs/v6_train/
rm -rf results/raw/SOTAV6_*
```
