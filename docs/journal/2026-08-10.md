# 2026-08-10 本周期：8卡H20，12点回收（~6.5h窗口）

> 写于 17:18。GPU 全空闲，无残留进程。
> 目标：判断v6 per-token counterfactual是否有学习信号，决定resume训练还是转向防守性实验。

## 0. 事实核验

| 项 | 结果 |
|---|---|
| GPU | 8× H20 全 0 MiB / 0% — 空闲 |
| 遗留进程 | 无 |
| v6 ckpt_best | 4 shard 均存在（各50 step） |
| v6 ckpt_step50 | 4 shard 均存在 |
| 数据路径 | navtest_nocot / metric_cache / AutoVLA_PDMS_89.ckpt 均 OK |

## 1. 历史退化轨迹

```
SFT scorer r=0.5 (LambdaRank):  PDMS = 0.894932 (shard0) / 0.89008 (full)
v3 Budget RL (proxy reward):    PDMS = 0.87066
v4 Budget RL (proxy reward):    PDMS = 0.86094
v5 True PDMS RL (2 epochs):     PDMS = 0.84850  ← 持续退化！
v6 Counterfactual (50 steps):   PDMS = 待产出
```

**退化根因分析**：
- v3→v5 的 RL 方案（REINFORCE + Gaussian budget）在不同 reward 设计下均退化
- v6 尝试 per-token counterfactual credit assignment，但 grad ≈ 0.001-0.003（单个 token 贡献太小）

## 2. v6 训练日志分析（50步）

- R 值：2.0-2.8（PDMS product ~0.67-0.93）
- kr：0.50-0.58（稳定）
- **grad_norm**：绝大部分步 ≈ 0.001-0.003（几乎为零）
  - 仅 step 50 出现异常 grad=16.421（loss=3.5764）
- 每步 ~96s（counterfactual 比 v5 慢 35%）

**判断**：grad 极小说明 per-token 的单个 token 边际 PDMS 贡献 < 0.001，counterfactual 方法在当前配置下难以产生有效学习信号。

## 3. 决策：两阶段判断

**Phase 1**：v6 ckpt_best shard0 quick eval (2949 scenes, GPU0, ~50min)
- 如果 PDMS > 0.890 → 有明显学习信号，resume v6 训练
- 如果 PDMS ∈ [0.88, 0.890] → 边际信号，可考虑 resume 但需更多 epoch
- 如果 PDMS < 0.88 → 无有效学习信号，转向防守性实验

**Phase 2 分支A（resume v6）**：
- 从 ckpt_step50 resume，继续训练 1 epoch（~5h，4 GPU）
- 训练完成后 eval full 4 shard（~3.6h）
- 但注意：当前 6.5h 窗口不够 train+eval（需要 ~9h）

**Phase 2 分支B（防守性实验）**：
- SFT scorer 完整 Pareto：r=0.25/0.5/0.75 full 4 shard（已有部分数据）
- τ-cut adaptive 完整曲线：kr040/050/060/070 full
- Matched-compute baseline：SFT r=0.355 vs Budget RL dynamic
- SparseVLM/ToMe/PruMerge Appendix baseline full eval

## 4. 启动

```bash
# 17:24 启动 v6 ckpt_best shard0 eval on GPU0
PID=6529, EXP=SOTAV6_R1_QUICK_sh0
```

## 5. 并行任务启动

### 5.1 Baseline full eval (GPU1-7, 18:02)
- SparseVLM r=0.5 full 4 shard (GPU1-4)
- PruMerge r=0.5 full 4 shard (GPU5-7, shard3 after completion)
- 脚本: `scripts/run_baselines_parallel.sh` (PID=19633)
- 7 jobs: PID 19677-19683, 正在加载数据

### 5.2 数据整理 (CPU)
- `docs/results/key_results.md` §15 已更新：完整full-navtest主表
- 发现：τ-cut全曲线和MSE scorer已完整，无需重跑

### 5.3 8卡状态 (18:05)
| GPU | 任务 | 状态 |
|---|---|---|
| 0 | v6 ckpt_best eval (464/2949) | 运行中, ~92% util |
| 1-4 | SparseVLM r=0.5 sh0-3 | 加载数据中 |
| 5-7 | PruMerge r=0.5 sh0-2 | 加载数据中 |

## 6. v6 ckpt_best eval 结果 (20:58)

**v6 ckpt_best (50 steps) shard0 PDMS = 0.87867** (N=2949, 0 failed)

| method | shard0 PDMS | Δ vs SFT |
|---|---|---|
| SFT scorer r=0.5 | 0.894932 | baseline |
| v4 BEST (proxy) | 0.878736 | −1.620 pt |
| v5 BEST (True PDMS) | 0.876668 | −1.826 pt |
| **v6 BEST (counterfactual 50 steps)** | **0.878672** | **−1.626 pt** |

**结论**：v6 per-token counterfactual 在50步后与v4/v5几乎无差异，无有效学习信号。
grad≈0 的观察被 eval 结果验证。

**决策**：不resume v6训练。转向防守性实验路线。
- 防守性实验大部分数据已存在（τ-cut全曲线、MSE scorer、所有核心baseline）
- SparseVLM/PruMerge full eval正在跑（GPU1-7，预计21:20完成）
- GPU0空闲，可用于额外实验

## 7. 后续计划 (21:00-24:00, ~3h)

1. 等待SparseVLM/PruMerge eval完成
2. 用GPU0跑额外实验（如：matched-compute SFT r=0.355 full，或其他Pareto点）
3. 聚合所有数据，更新key_results.md §15
4. 整理ICLR论文需要的完整数据表
5. 23:50前备份+journal收尾

## Reverse 指令

如果 v6 PDMS < 0.88：
```bash
# Kill v6 eval (如果还在跑)
kill 6529
# 转向防守性实验（见分支B）
# 实际：防守性实验大部分数据已存在，baseline并行eval已启动
```
